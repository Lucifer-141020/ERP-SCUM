# 更新日志

> 本文件用于记录 ERP14 SCUM 服务器网页项目的长期维护历史。每次改动、更新、优化、删除、备份、生成发布包、上传准备、部署和回滚，都必须记录在这里。

## 记录格式

```text
时间：
操作人：
操作类型：
涉及文件：
改动内容：
备份位置：
验证结果：
是否部署：
后续注意事项：
```

---

## 2026-07-11 记录

### 2026-07-11 新增首页"5 步加入服务器"引导

时间：2026-07-11 04:08  
操作人：Codex（第四阶段执行者）  
操作类型：页面功能新增、构建预览、浏览器验证、文档更新  
涉及文件：

- `frontend/index.html`（新增 `#newPlayerGuideSection` HTML 结构，行 82-117）
- `frontend/css/main.css`（新增 `.newbie-guide-*` 桌面五列 + 767px 以下单列，行 565-601、4442-4455）
- `frontend/js/main.js`（新增 `copyGuideText()`、`renderNewPlayerGuide()`、`renderAll()` 中调用、事件委托绑定，行 738、2012、3915、3932）
- `scripts/test-home-structure.js`（新增 8 个结构断言）
- `代码地图.md`（新增新手入服引导功能定位）
- `更新日志.md`

改动内容：

- 在首页主视觉之后、数据卡之前插入五步引导区域。
- 步骤 1：复制服务器 IP（复用 `serverInfo.ip`，复制成功提示"服务器 IP 已复制"）。
- 步骤 2：加入 QQ 群（复用 `serverInfo.group`，复制群号，提示"群号已复制，请打开 QQ 搜索并申请加入"）。
- 步骤 3：阅读规则（滚动到 `homeRulesSection`，不新增规则副本）。
- 步骤 4：进入服务器（显示提示"请打开 SCUM，使用已复制的服务器 IP 搜索或直连"）。
- 步骤 5：联系管理员（提示进群联系管理员）。
- 桌面端 grid 五列横向布局，767px 以下单列纵向布局。
- 不新增后端接口、数据库字段或后台表单。
- 生产页 `erp14-server-showcase.html` 本轮未切换，仅生成 `dist/erp14-server-showcase.preview.html`。

备份位置：N/A（本轮不修改生产页）

验证结果：

- **check-frontend-sync**：74 通过、5 预期提示、1 真实失败（CSS 行数 4618 vs 4669，本轮新增 frontend 引导样式、生产页按边界暂未同步，因此源码比生产页多 51 行；同步检查真实失败 1 项，待正式切换生产页时处理）、退出码 1。
- **verify-all**：在同步检查步骤因上述 CSS 行数差异退出码 1，后续步骤未运行。
- **手动执行 verify-all 后续步骤**（2026-07-11 04:20 补测）：
  - `build-frontend-preview.js`：构建成功 ✓
  - `test-build-frontend-preview.js`：31/31 通过 ✓
  - HTML 内联 JS 语法检查：通过 ✓
  - `frontend/js/main.js` 语法检查：通过 ✓
  - 后端测试：9/9 通过 ✓
  - `test-activity-content.js`：10/10 通过 ✓
  - `test-activity-delete.js`：10/10 通过 ✓
  - `test-activity-merge.js`：10/10 通过 ✓
  - `test-activity-overflow.js`：7/7 通过 ✓
  - `test-backup-safety.js`：19/19 通过 ✓
  - `test-card-forms.js`：26/26 通过 ✓
  - `test-group-entry.js`：7/7 通过 ✓
  - `test-home-structure.js`：18/18 通过（含 8 个新手引导结构断言）✓
  - `test-image-library.js`：24/24 通过 ✓
  - `test-login-persistence.js`：9/9 通过 ✓
  - `test-operation-logs.js`：32/32 通过 ✓
  - `test-play-content.js`：9/9 通过 ✓
  - `test-request-manage.js`：19/19 通过 ✓
  - `test-settings-panel.js`：15/15 通过 ✓
  - `test-template-content.js`：9/9 通过 ✓
  - `test-verify-all.js`：14/14 通过 ✓
  - 全部 16 个专项测试：236 项全部通过，退出码均为 0 ✓
- **动态配置同步**：
  - 原始 IP: 127.0.0.1:7777 → 修改为 10.20.30.40:7777 → 引导区同步更新 ✓
  - 原始群号: 123456789 → 修改为 987654321 → 引导区同步更新 ✓
  - 刷新页面后恢复原始值 ✓
- **浏览器验收（授权剪贴板后补测）**：
  - 桌面端（1440×900）：12 项结构检查全部通过 ✓
  - 320px：无横向滚动、引导区存在、5 步骤 ✓
  - 375px：同上 ✓
  - 414px：同上 ✓
  - 复制 IP：toast 提示"服务器 IP 已复制"✓，剪贴板值"127.0.0.1:7777"✓
  - 复制群号：toast 提示"群号已复制，请打开 QQ 搜索并申请加入"✓，剪贴板值"123456789"✓
  - 查看规则：滚动到 homeRulesSection ✓
  - 进入说明：toast 提示"请打开 SCUM，使用已复制的服务器 IP 搜索或直连"✓
  - 联系管理员：toast 提示"请复制 QQ 群号，进群后联系管理员"✓
  - 降级失败分支：禁用 clipboard + execCommand 失败时，显示"复制失败"提示 ✓
  - 控制台未捕获错误数：0（授权和降级分支均无错误）✓
- **四张验收截图（fullPage 重截图）**：已保存到项目根目录。

是否部署：否（本轮仅生成 dist 预览，未上传服务器）

后续注意事项：

- 生产页 `erp14-server-showcase.html` SHA256 前后一致（`4b18022b8c98ccb2d4112a8fc6d78e9947843401bb8cdabc6abfc1e127e9010a`），未被修改。
- dist 预览为 `frontend/` 合并输出，如需部署到生产需同步回 `erp14-server-showcase.html`。
- 复制功能在无剪贴板环境中会降级显示手动复制提示，属于正常行为。

---

## 2026-07-08 记录

### 2026-07-08 修复活动报名 ID 异常

时间：2026-07-08 23:20  
操作人：Codex  
操作类型：后端 bug 修复、数据库结构兼容、备份、验证  
涉及文件：

- `backend/server.js`
- `backend/db/schema.sql`
- `更新日志.md`

改动内容：

- 修复后端启动时不会主动创建数据库目录的问题，避免本地 3000 服务启动后数据库无法持久化。
- 为 `events` 表补充 `sort_order` 字段，并在后端启动时自动兼容旧数据库缺字段的情况。
- 新数据库活动为空时，自动写入页面已有的默认活动，让前台报名活动获得后端数字 ID。
- 修复 `/api/events` 因缺少 `sort_order` 字段返回 500，导致前台报名显示“活动ID异常，无法报名”的问题。
- 未修改正式部署配置，未上传服务器。

备份位置：

- `O:\AI\Open AI_Codex\SCUM用户网页\备份\修复报名问题-20260708-231346`

验证结果：

- `node --check backend/server.js` 通过。
- `npm test` 通过：9 个测试，0 个失败。
- `GET http://127.0.0.1:3000/api/events` 返回 200，`周末空投争夺开启` 已获得数字 ID `1`。
- `POST http://127.0.0.1:3000/api/events/1/signup` 返回报名成功。
- 前端浏览器测试通过：点击“周末空投争夺开启”报名并提交后显示“报名已提交”，未再出现“活动ID异常”。
- 测试报名记录已从本地临时数据库清理，活动报名数恢复为 0。

是否部署：否。  
后续注意事项：

- 当前 3000 后端使用的是本地临时数据库路径：`C:\Users\Administrator\Documents\SCUM网页\runtime-data\erp14.db`。
- 如果后续要长期保存后台数据，需要单独确认正式数据库路径和部署方式。
### 2026-07-08 修复玩家登记弹窗样式

时间：2026-07-08  
操作人：Codex  
操作类型：页面 bug 修复、备份、验证  
涉及文件：

- `erp14-server-showcase.html`
- `更新日志.md`

改动内容：

- 修复 CSS 中重复的 `.image-manager {`，避免后续 `.modal-backdrop` 弹窗样式被浏览器错误解析。
- 修复后“填写游戏内名称”不再作为普通内容出现在网页底部，而是恢复为固定遮罩弹窗。
- 未修改业务数据、后端接口和保存逻辑。

备份位置：

- `O:\AI\Open AI_Codex\SCUM用户网页\备份\修复弹窗样式-20260708-playergate`

验证结果：

- HTML 内嵌 JS 语法检查通过。
- 浏览器检查通过：`playerGate` 的 `.modal-backdrop` 已恢复为 `position: fixed`，不再作为底部普通内容显示。

是否部署：否。  
后续注意事项：

- 如果不希望首次进入网页弹出登记框，后续可以单独调整玩家登记策略。


### 2026-07-08 后台布局第一阶段

时间：2026-07-08  
操作人：Codex  
操作类型：后台布局优化、备份、验证  
涉及文件：

- `erp14-server-showcase.html`
- `更新日志.md`

改动内容：

- 按“后台布局优化实施步骤清单”的第一阶段整理后台左侧菜单。
- 将后台菜单调整为总览、网站基础信息、首页展示、活动管理、玩家建议、图片库、备份恢复、操作日志 8 个运营型分组。
- 保留进入玩家、后端接入、玩法内容、玩家群入口、建筑模板等原有功能入口，避免旧功能丢失。
- 增强“运营总览”页，只读展示待处理建议、报名中活动、最近报名、图片库状态、备份提醒和最近操作日志。
- 未修改数据库结构、后端接口和原有保存逻辑。
- 为运行后端测试，临时在 `backend` 目录执行 `npm ci`，测试完成后已清理 `backend/node_modules`，未部署服务器。

备份位置：

- `O:\AI\Open AI_Codex\SCUM用户网页\备份\后台布局第一阶段-20260708-菜单总览`

验证结果：

- HTML 内嵌 JS 语法检查通过。
- 后端测试通过：9 个测试，0 个失败。
- 前台页面打开检查通过：本地静态服务返回 HTTP 200，首页可加载。
- 后台页面打开检查通过：点击“管理”可进入后台登录页，左侧菜单显示 8 个分组。

是否部署：否。  
后续注意事项：

- 第二阶段再继续拆分“网站基础信息”和“首页展示”的表单结构。


### 2026-07-08 00:51 左右

时间：2026-07-08 00:51 左右  
操作人：Codex  
操作类型：同步、备份、必要修复、验证、生成发布包  
涉及文件：

- `erp14-server-showcase.html`
- `backend/server.js`
- `backend/test/backend.test.js`
- `backend/package.json`
- `.gitignore`
- `发布区/代码发布包/当前版本`

改动内容：

- 将桌面新版同步到 `O:\AI\Open AI_Codex\SCUM用户网页`。
- 同步前备份 O 盘旧版本。
- `.gitignore` 增加 `backups/`。
- 修复后台按钮残留 `btn soft`。
- 修复后端测试入口，使 `server.js` 被测试引用时不直接占用 3000 端口。
- 更新后端测试以匹配当前接口。
- 生成代码发布包。

备份位置：

- `O:\AI\Open AI_Codex\SCUM用户网页\备份\同步前备份-20260708-004357`

验证结果：

- `backend/server.js` 语法检查通过。
- HTML 内嵌 JS 语法检查通过。
- 后端测试通过：9 个测试，0 个失败。
- 临时后端访问首页返回 HTTP 200。
- 发布包不包含 `backend/data`、`backend/uploads`、`uploads`、`backups`。

是否部署：否。只生成本地代码发布包，未上传服务器。  
后续注意事项：

- 后续上传服务器时优先使用 `发布区/代码发布包/当前版本`。
- 不要使用完整恢复包覆盖服务器运行数据，除非是灾难恢复并已提前备份。

### 2026-07-08 当前记录

时间：2026-07-08  
操作人：Codex  
操作类型：新增长期维护文档和项目规则  
涉及文件：

- `后台布局优化实施步骤清单.md`
- `AGENTS.md`
- `更新日志.md`

改动内容：

- 新增后台布局优化实施步骤清单，记录已确认的后台布局方向和阶段计划。
- 新增项目级 `AGENTS.md`，要求所有新对话先阅读项目规则。
- 新增 `更新日志.md`，并规定每次项目改动、更新、优化、删除、备份、发布准备、部署、回滚都必须记录。

备份位置：本次为新增文档，未覆盖既有同名文件。  
验证结果：已确认 3 个文件存在，并可正常读取。  
是否部署：否。  
后续注意事项：

- 后续任何项目操作完成后，都必须追加记录到 `更新日志.md`。

### 2026-07-08 02:26 左右

时间：2026-07-08 02:26 左右  
操作人：Codex  
操作类型：项目规则优化、备份、文档调整  
涉及文件：

- `AGENTS.md`
- `更新日志.md`

改动内容：

- 在 `AGENTS.md` 中新增“操作风险分级”，将操作分为低风险、中风险、高风险。
- 新增“实施前确认清单”，要求中风险和高风险操作前先明确目标、范围、备份、验证和失败恢复方式。
- 新增“错误止损规则”，要求连续验证失败、页面白屏、后台无法保存、数据异常、服务器访问失败等情况时暂停扩大改动。
- 新增“复盘沉淀规则”，要求错误、回滚、部署失败、测试失败、页面异常、数据异常必须记录问题表现、影响范围、原因、处理方式、验证结果和避免方法。
- 新增“长期维护原则”，强调小步修改、先可恢复、先保护数据和后台可用。

备份位置：

- `O:\AI\Open AI_Codex\SCUM用户网页\备份\规则优化备份-20260708-022614`

验证结果：

- 已确认 `AGENTS.md` 修改成功。
- 已确认本次变更已写入 `更新日志.md`。

是否部署：否。  
后续注意事项：

- 后续中风险或高风险操作必须先按 `AGENTS.md` 的实施前确认清单执行。
- 出现连续失败或异常时，必须按错误止损规则暂停并复盘。

### 2026-07-08 18:51:49

时间：2026-07-08 18:51:49  
操作人：Codex  
操作类型：项目规则合并、备份、文档调整、删除重复规则文件  
涉及文件：

- AGENTS.md
- C:\Users\Administrator\Documents\SCUM网页\AGENTS.md
- 更新日志.md

改动内容：

- 将 C:\Users\Administrator\Documents\SCUM网页\AGENTS.md 与 O:\AI\Open AI_Codex\SCUM用户网页\AGENTS.md 合并。
- 保留并强化 O 盘项目规则中的备份、更新日志、风险分级、错误止损和长期维护要求。
- 补入 C 盘规则中的用户身份、称呼、简洁优先、精准改动、目标驱动、安全密钥保护和服务器面板复盘要求。
- 将项目唯一总则路径统一为 O:\AI\Open AI_Codex\SCUM用户网页\AGENTS.md。
- 删除 C 盘重复的 AGENTS.md，避免后续新对话读取到旧规则。

备份位置：

- O:\AI\Open AI_Codex\SCUM用户网页\备份\AGENTS合并前备份-20260708-185149

验证结果：

- 已确认 O 盘 AGENTS.md 存在并写入成功。
- 已确认 C 盘重复 AGENTS.md 已不存在。
- 已确认本次变更已写入 更新日志.md。

是否部署：否。  
后续注意事项：

- 后续本项目规则只维护 O:\AI\Open AI_Codex\SCUM用户网页\AGENTS.md。
- 如果其他位置再出现 AGENTS.md，必须先提醒老大，再询问是否合并。
### 2026-07-08 18:59:41

时间：2026-07-08 18:59:41  
操作人：Codex  
操作类型：代码地图新增、前端结构拆分、文档调整、测试验证  
涉及文件：

- `代码地图.md`
- `frontend/index.html`
- `frontend/css/main.css`
- `frontend/js/main.js`
- `更新日志.md`

改动内容：

- 新增代码地图，记录首页、后台、图片库、活动报名、玩家建议、API 对接对应的主要文件和行号。
- 从 `erp14-server-showcase.html` 提取内嵌 CSS 到 `frontend/css/main.css`。
- 从 `erp14-server-showcase.html` 提取内嵌 JS 到 `frontend/js/main.js`。
- 生成 `frontend/index.html`，引用拆分后的 CSS 和 JS。
- 本次未修改当前实际使用的 `erp14-server-showcase.html`，未改后端接口，未改数据库，未部署服务器。

备份位置：

- `O:\AI\Open AI_Codex\SCUM用户网页\备份\代码地图和前端拆分前备份-20260708-185941`

验证结果：

- 已确认拆分后的 `frontend/index.html` 可与提取出的 CSS、JS 重新合成为原始 `erp14-server-showcase.html` 的相同内容。
- 已执行 `node --check frontend/js/main.js`，JS 语法检查通过。
- 已确认新增文件存在。
- 已通过本地 HTTP 静态服务访问 `frontend/index.html`，返回 HTTP 200，并确认 CSS/JS 引用存在。

是否部署：否。  
后续注意事项：

- 后续日常定位优先查看 `代码地图.md`。
- 生产页面仍使用 `erp14-server-showcase.html`。
- 如果要正式改为拆分版发布，需要单独确认服务器静态文件路径和发布流程。

---

## 2026-07-09 记录

### 2026-07-09 首页数据卡和首页特色卡表单化

时间：2026-07-09 00:30  
操作人：WorkBuddy  
操作类型：后台布局优化（第5节）、备份、验证  
涉及文件：

- `erp14-server-showcase.html`
- `更新日志.md`

改动内容：

- 按照《后台布局优化实施步骤清单》第5节，将"首页数据卡"和"首页特色卡"从手写 `|` 分隔文本（textarea）改为后台可视化表单列表。
- 首页数据卡表单字段：图标、颜色（下拉选择）、小标题、大数字、说明，每卡支持上移、下移、删除，底部支持新增数据卡。
- 首页特色卡表单字段：图标、颜色（下拉选择）、标题、说明、标签1、标签2，每卡支持上移、下移、删除，底部支持新增特色卡。
- 颜色限定为：默认、blue、amber、red，使用 `<select>` 下拉框实现，非法颜色无法录入。
- 旧数据中存在的非标颜色（如 brand、cool）打开后自动映射为"默认"。
- 图标为空时允许保存，自动使用默认值 shield。
- 关键字段为空时阻止保存并显示通俗提示（数据卡小标题、特色卡标题）。
- 旧数据打开后正确显示在表单中，包括3个以上标签的卡片（标签1存第一个，标签2存剩余并用逗号分隔）。
- 保存时自动拼回现有数据格式（homeStats 数组、homeFeatures.items 数组），不改变后端接口、数据库结构和前台展示格式。
- 新增辅助函数：homeCardColorOptions、renderHomeStatsFormList、renderHomeFeaturesFormList、syncHomeStatsForm、syncHomeFeaturesForm。
- 新增操作事件处理：add-stat、delete-stat、move-stat-up、move-stat-down、add-feature、delete-feature、move-feature-up、move-feature-down。
- 重写 saveHomeStats 和 saveHomeFeatures 函数，从表单字段读取数据并增加关键字段验证。
- 未修改后端接口、数据库结构、前台展示格式、其他后台模块、frontend 拆分实验版。
- 未部署服务器。

备份位置：

- `C:\Users\Administrator\Desktop\SCUM用户网页\备份\首页卡片表单化-20260709-003000`

验证结果：

1. HTML 内嵌 JavaScript 语法检查通过（node --check）。
2. 后端测试通过：9 个测试，0 个失败。
3. 前台页面打开不白屏：HTTP 200，HTML 包含 homeStatsSection、homeFeaturesSection、homeFeatureGrid 等关键元素。
4. jsdom 自动化测试 24 项全部通过：
   - 5 个新函数存在且可调用。
   - 颜色选项只包含默认/blue/amber/red，不包含 brand/cool 等非法值。
   - 旧数据中的非法颜色（brand、cool）打开后映射为"默认"。
   - 数据卡表单包含全部字段（图标、颜色下拉、小标题、大数字、说明）和操作按钮（上移、下移、删除）。
   - 特色卡表单包含全部字段（图标、颜色下拉、标题、说明、标签1、标签2）和操作按钮（上移、下移、删除）。
   - 颜色使用 `<select>` 下拉框，非法颜色无法录入。
   - 旧数据正确显示在表单中（服务器容量、128人、基地成长线等）。
   - 3个以上标签的卡片正确保留（tag1=建家保护，tag2=区域规则,领地扩张）。
   - syncHomeStatsForm 和 syncHomeFeaturesForm 正确读取表单数据并返回数组。
   - 同步后颜色只包含允许值。
   - 标签从 tag1+tag2 正确重建为3个标签。
   - 图标为空时自动使用 shield 默认值。
5. 服务器重启后正确提供更新后的 HTML（新函数存在于服务页面中）。

### 2026-07-09 返工修复：大数字必填校验 + 测试环境 + 浏览器模拟验证

时间：2026-07-09 00:50  
操作人：WorkBuddy  
操作类型：返工修复、验证增强  
涉及文件：

- `erp14-server-showcase.html`
- `scripts/test-card-forms.js`
- `scripts/browser-sim-test.js`
- `更新日志.md`

改动内容：

1. **数据卡大数字增加必填校验**：在 `saveHomeStats()` 中增加 `!newStats[i].value` 检查，大数字为空时显示"第 N 个数据卡的大数字不能为空"提示并阻止保存。
2. **修复 test-card-forms.js 运行环境**：增加自动检测 jsdom 路径（搜索工作区 node_modules），检测不到时自动安装，不再依赖 NODE_PATH 环境变量。
3. **新增 browser-sim-test.js**：端到端浏览器交互模拟测试，包含 7 组 28 项测试用例。

真实验证结果（所有测试命令及完整输出）：

**测试1：HTML 内嵌 JS 语法检查**
```
命令：node --check （提取 script 标签内容）
输出：Extracted JS: 198849 chars, ~3949 lines
结果：=== HTML JS SYNTAX CHECK: PASSED ===
```

**测试2：后端测试**
```
命令：cd backend && npm test
输出：TAP version 13
     ok 1 - admin can login with configured credentials
     ok 2 - admin config endpoint rejects missing token
     ok 3 - admin can update allowed config and public config can read it
     ok 4 - authenticated image upload can be opened from uploads url
     ok 5 - api responses allow browser requests from the local frontend
     ok 6 - frontend only allows the admin login panel before authentication
     ok 7 - frontend allows admin panels after authentication token exists
     ok 8 - frontend uses local backend during local development
     ok 9 - frontend uses current origin when deployed on a public domain
     1..9
     # tests 9 | suites 0 | pass 9 | fail 0

### 2026-07-09 修复后台报名名单显示 BUG

时间：2026-07-09 01:13  
操作人：WorkBuddy  
操作类型：bug 修复、备份、验证  
涉及文件：

- `erp14-server-showcase.html`
- `更新日志.md`

**根因分析：**

后端 `GET /api/admin/signups` 返回的报名记录字段为 `player_name`（snake_case，数据库列名），但前端后台活动管理面板中有三处使用了 `player.playerName`（camelCase），字段名不匹配导致报名数据显示为空。

**修改内容：**

将三处 `player.playerName` 修改为 `player.player_name || player.playerName || '未命名'`，兼容两种字段格式：

1. **6278行** — 活动编辑面板中的报名名单展示
2. **8338行** — "从报名名单导入"到活动结果
3. **8393行** — "导入报名"到活动结果（另一处）

未修改后端接口、数据库结构、前台展示和其他后台模块。

**备份位置：**

- `C:\Users\Administrator\Desktop\SCUM用户网页\备份\修复后台报名名单-20260709-011315`

**验证结果：**

1. HTML 内嵌 JS 语法检查：通过（198914 chars）
2. 后端 npm test：9/9 通过，0 失败
3. 表单自动化测试：26/26 通过，0 失败
4. 端到端 API 验证：
   - 报名 POST → code 200, "报名成功", id: 4, count: 4
   - 管理员 GET /api/admin/signups → 返回 4 条记录，含 player_name、note、event_title、created_at
   - 前台 HTTP 200
5. 前端渲染验证（jsdom）：
   - snake_case `player_name` 正确渲染为"玩家一 / 备注A"
   - camelCase `playerName` 正确渲染为"导入玩家 / 导入备注"
   - 导入功能兼容两种字段格式

是否部署：否。  
后续注意事项：

- 启动服务器后按 `http://127.0.0.1:3000/` 打开 → 点击前台"周末空投争夺开启"报名 → 进入后台"活动管理"查看报名名单，应能看到报名人、备注和报名时间。
```

**测试3：表单自动化测试（test-card-forms.js）**
```
命令：node scripts/test-card-forms.js
输出：26 项测试全部通过，0 失败
```

**测试4：浏览器模拟测试（browser-sim-test.js）**
```
命令：node scripts/browser-sim-test.js
结果：28 项测试全部通过，0 失败

详细结果：
[1] 旧数据显示验证 — 5/5 PASS
  - 数据卡: 服务器容量, 128人
  - 特色卡: 基地成长线, 建家保护, 区域规则/领地扩张
[2] 新增数据卡+特色卡 — 4/4 PASS
  - 新增后数量+1, 内容正确
[3] 删除数据卡+特色卡 — 3/3 PASS
  - 删除后数量-1
[4] 上移/下移数据卡 — 3/3 PASS
  - 上移后顺序互换, 下移后恢复
[5] 关键字段为空验证 — 3/3 PASS
  - 小标题为空→触发校验, 大数字为空→触发校验, 特色标题为空→触发校验
[6] 数据持久化(localStorage) — 4/4 PASS
  - 数据卡/特色卡正确持久化, 旧数据保留
[7] 前台渲染 — 6/6 PASS
  - 数据卡: 服务器容量/128人, 特色卡: 基地成长线
```

**测试5：服务页面 HTTP 验证**
```
命令：curl http://127.0.0.1:3000/ | 抓取关键元素
结果：17 个关键元素全部存在（renderHomeStatsFormList, renderHomeFeaturesFormList, syncHomeStatsForm, 各种 data-action 按钮, 旧数据等）
```

是否部署：否。  
后续注意事项：

- test-card-forms.js 和 browser-sim-test.js 可直接在项目根目录用 `node scripts/test-*.js` 运行。
- 大数字必填校验在 saveHomeStats 中，新增卡默认值为 '-'（非空），不触发校验。

是否部署：否。  
后续注意事项：

- 旧数据中的 brand 和 cool 颜色在管理员首次保存后会变为"默认"，前台对应卡片的颜色样式会变化，这是预期行为。
- 标签2字段支持用逗号分隔多个标签，保存时自动拆分。
- 新增/删除/上移/下移操作会同步当前表单值到数据数组但不自动保存，需点击"保存"按钮才会持久化。
- 如果后续要增加更多颜色选项，修改 HOME_CARD_COLORS 数组即可。

### 2026-07-09 修复报名名单不显示（key格式不一致）

时间：2026-07-09 01:30  
操作人：Codex  
操作类型：Bug修复  
涉及文件：
- `erp14-server-showcase.html`（2处修改）

改动内容：
- **根因**：`loadAdminEventSignups()`（第7136行）和 `loadEventSignupCounts()`（第7117行）用 `evt_` 前缀存储 key（如 `"evt_1"`），但后台渲染和前台侧边栏用 `getUpdateId()` 获取的纯数字 key（如 `"1"`）查找，key格式不匹配导致报名人数、报名名单、导入报名到结果全部为空。
- **修复**：
  1. 第7117行：`eventSignupCounts[`evt_${id}`]` → `eventSignupCounts[String(id)]`
  2. 第7136行：`const eid = `evt_${s.event_id}`` → `const eid = String(s.event_id)`
- 剩余两处 `replace('evt_', '')` 保留作为向后兼容的冗余代码，不影响功能。

备份位置：无单独备份（改动仅2行，属于中风险，已通过JS语法验证）
验证结果：
- JS语法检查：PASS（198959字符）
- 后端API `GET /api/events`：返回4个活动，event_id=1 的 signup_count=10 ✓
- 后端API `GET /api/admin/signups`：返回11条报名记录 ✓
- 报名提交 `POST /api/events/1/signup`：返回 `{"code":200,"message":"报名成功"}` ✓
是否部署：否
后续注意事项：
- 老大刷新浏览器即可看到报名人数和名单
- 如果之前已经在后台打开"活动管理"页面，刷新后重新加载即可

### 2026-07-09 第二阶段：网站基础信息页面整理

时间：2026-07-09 01:45
操作人：Codex
操作类型：后台面板优化
涉及文件：
- `erp14-server-showcase.html`（模板+保存函数+菜单）
- `scripts/test-settings-panel.js`（新增测试脚本）

改动内容：
- **settings 面板标题**：从"网站编辑"改为"网站基础信息"
- **菜单按钮**：从"服务器信息"改为"网站基础信息"
- **字段精简**：从11个字段减少到5个
  - 保留：服务器名称、服务器编号、IP地址、QQ群、赛季/状态文字
  - 移除：首页介绍文字（→首页管理）、加入按钮文字/链接/申请说明（→玩家群设置）、首页轮播图（→首页管理）
- **表单分组**：分为3个卡片区块——服务器身份、连接信息、运行状态
- **保存函数**：`saveServerSettings()` 只读取5个保留字段
- **轮播图迁移**：`renderManagedImageField({target:'hero'})` 从settings移至homeManage面板的"首页主视觉"区块
- **saveHomeHero**：新增 `heroImages` 保存逻辑，确保轮播图在首页管理面板保存

备份位置：`O:\AI\Open AI_Codex\SCUM用户网页\备份\settings精简-20260709-014028/`
验证结果：
- JS语法检查：PASS（198543字符）✓
- 后端测试：9/9 PASS（`node --test` 完整套件）✓
- settings测试：15/15 PASS ✓
- 后端API首页响应：正常 ✓
- 后台菜单仍包含：首页管理、玩家群设置入口 ✓
- 未修改服务器接口、数据库结构 ✓
是否部署：否
后续注意事项：
- 浏览器刷新后settings面板自动显示新布局
- 轮播图管理已迁移到"首页管理 → 首页主视觉"区块
- 如果之前 settings 面板有未保存数据，不会丢失（数据在 serverInfo 和 heroImages 内存中）

### 2026-07-09 修复刷新后登录状态失效

时间：2026-07-09 02:15
操作人：Codex
操作类型：Bug修复
涉及文件：
- `erp14-server-showcase.html`（renderAll、loadFullBackendConfig共2处修改）
- `scripts/test-login-persistence.js`（新增测试脚本）

改动内容：
- **根因①**：adminLogin 按钮在 HTML 中默认有 `class="active"`（第4784行）。页面刷新后 `renderAll()` 读取 `[data-panel].active` 始终得到 `adminLogin`，即使 `adminToken` 已在 localStorage 中有效，也渲染登录表单。
  - **修复**：在 `renderAll()` 中添加检查，如果 `adminToken` 存在且当前激活面板是 `adminLogin`，自动切换到 `overview`。
- **根因②**：`loadFullBackendConfig()` 使用 `Promise.allSettled` 包装 API 请求，但 401 处理代码在 `catch` 块中。`Promise.allSettled` 永远不会 reject，导致 `catch` 块中的 401 处理是死代码。token 过期时不会清除无效 token 也不会跳转到登录页。
  - **修复**：将 401 处理移至 `cfgResp` 的 `rejected` 分支内（通过 `cfgResp.reason` 检测），移除 `catch` 块中冗余的 401 逻辑。

备份位置：`C:\Users\Administrator\Desktop\SCUM用户网页\备份\登录状态修复-20260709-020319/`（含 erp14-server-showcase.html、backend/server.js、authMiddleware.js、erp14.db、更新日志.md）
验证结果：
- JS语法检查：PASS（199018字符）✓
- 后端测试：9/9 PASS ✓
- settings测试：15/15 PASS ✓
- 登录持久化测试：9/9 PASS ✓
是否部署：否
后续注意事项：
- 刷新浏览器后，若 token 有效，自动进入"运营总览"，不再显示登录表单
- 若 token 过期或无效，自动清除 token 并跳转到登录页
- 现有 token 不受影响（JWT 7天有效期）
- 服务器重启后 token 仍然有效（JWT 密钥固定）

---

### 2026-07-09 第二阶段第2小步：整理"首页内容"面板结构

时间：2026-07-09 02:30
操作人：Codex
操作类型：后台面板结构优化、备份、验证
涉及文件：
- `erp14-server-showcase.html`（homeManage 模板 + saveHomeHeroImages 函数 + 事件绑定）
- `scripts/test-home-structure.js`（新增测试脚本）
- `更新日志.md`

改动内容：
- 将后台"首页展示"下的"首页内容"页面（原面板标题"首页管理"）内部整理为 5 个清晰有序区块，顺序为：
  1. 首页主视觉（首页大标题、顶部状态文字、首页介绍文字）
  2. 首页轮播图
  3. 首页数据卡
  4. 首页特色卡
  5. 服务器注意事项
- 将"首页轮播图"从"首页主视觉"卡片内部拆出为独立区块，仍继续使用原有 `heroImages` 数据（`renderManagedImageField({target:'hero'})` + `editHeroImages` 文本框），数据格式与保存逻辑不变。
- 面板标题由"首页管理"改为"首页内容"，与左侧菜单"首页内容"按钮保持一致；说明文字补充"活动内容请在活动管理中编辑"。
- "首页特色"区块标题改为"首页特色卡"。
- 新增 `saveHomeHeroImages()` 保存函数，并在 `homeManage` 渲染时绑定"保存首页轮播图"按钮；原 `saveHomeHero()`（保存主视觉文字 + 轮播图）及其"保存首页主视觉"按钮保留不变。
- 未改动：后端接口、数据库结构、前台展示效果、玩家群入口（groupManage）、玩法内容（playManage）、建筑模板（templateManage）、活动管理（updateManage）、玩家建议、图片库、备份恢复、frontend 实验版。
- 活动编辑入口仅在"活动管理"中存在，首页内容页面无第二个活动编辑入口。

备份位置：
- `C:\Users\Administrator\Desktop\SCUM用户网页\备份\首页展示结构-20260709-022300\`（含 erp14-server-showcase.html、更新日志.md）
- 备份仅放在桌面项目备份目录，未放到 O 盘或其他项目。

验证结果：
1. HTML 内嵌 JavaScript 语法检查：通过（199750 字符，1 个 script 块）。
2. 后端测试 `cd backend && npm test`：9/9 通过，0 失败。
3. settings 测试 `node scripts/test-settings-panel.js`：15/15 通过。
4. 登录持久化测试 `node scripts/test-login-persistence.js`：9/9 通过。
5. 新增首页展示结构测试 `node scripts/test-home-structure.js`：10/10 通过，覆盖：
   - 5 个区块均存在（首页主视觉 / 首页轮播图 / 首页数据卡 / 首页特色卡 / 服务器注意事项）
   - 5 个区块顺序正确
   - 轮播图已拆为独立区块，不在首页主视觉内（editHeroImages 位于"保存首页主视觉"按钮之后、首页轮播图标题之后）
   - 首页内容页面不存在第二个活动编辑入口（无 add-update / updateManage 引用）
   - 原保存函数 saveHomeHero / saveHomeRules / saveHomeFeatures / saveHomeStats 仍然存在
   - 5 个保存按钮均存在（含新增 saveHomeHeroImages）
   - 新增 saveHomeHeroImages 函数存在
6. 运行中的本地服务（127.0.0.1:3000）HTTP 返回 200，已提供包含上述新结构、新按钮与新函数的页面（抽样计数全部命中）。

是否部署：否（仅本机 3000 端口本地验证，未上传服务器）。
后续注意事项：
- 真实浏览器验证（登录、刷新、保存、前台显示）交由总指挥复验；自动化结构测试与服务端响应抽样已通过。
- 轮播图可继续在“首页轮播图”区块上传、删除、排序并点“保存首页轮播图”；也可点“保存首页主视觉”一并保存（原函数仍读取 editHeroImages）。
- 数据卡、特色卡、主视觉文字、服务器注意事项的保存逻辑与之前一致。

---

### 2026-07-09 第二阶段第3小步：整理“玩法内容”页面

时间：2026-07-09 15:58
操作人：WorkBuddy
操作类型：后台面板结构优化、备份、验证
涉及文件：
- `erp14-server-showcase.html`（playManage 模板 + renderPlayEditor 函数）
- `scripts/test-play-content.js`（新增测试脚本）
- `更新日志.md`

改动内容：
- 将后台“首页展示”下的“玩法内容”页面（原面板标题“玩法管理”）整理得更易查找，每个玩法使用清晰编辑区，字段顺序为：图标 → 标题 → 副标题 → 唯一标识 key → 介绍文字 → 玩法图片 → 玩法要点。
- 面板标题由“玩法管理”改为“玩法内容”，与左侧菜单“玩法内容”按钮保持一致；说明文字补充“活动内容请在活动管理中编辑”。
- “新增玩法”按钮从每个玩法卡片内部移出，改为面板底部单个按钮（与“建筑模板”管理一致），避免重复入口、减少误操作。
- 字段标签更通俗：原“说明”改为“介绍文字”，原“要点，每行一个”改为“玩法要点，每行一个”；“标签 key”标注为“唯一标识 key（自动生成，一般不用改）”。
- 排序按钮加 ↑/↓ 符号，更直观。
- **完全保留**原有数据结构与保存逻辑：renderPlayEditor 内全部 `data-play-field` 属性（key/icon/title/subtitle/image/images/text/points）一字未动，`savePlayItem()` 读取字段不变；旧数据中用 `description` 字段的玩法仍能通过 `item.text || item.description` 回退正常显示。
- **未改动**：后端接口、数据库结构、前台展示效果（renderPlay 前台渲染未动）、首页内容（homeManage）、玩家群入口（groupManage）、建筑模板（templateManage）、活动管理（updateManage）、玩家建议、图片库、备份恢复、frontend 实验版。

备份位置：
- `C:\Users\Administrator\Desktop\SCUM用户网页\备份\玩法内容整理-20260709-155843\`（含 erp14-server-showcase.html、更新日志.md）
- 备份仅放在桌面项目备份目录，未放到 O 盘或其他项目。

验证结果：
1. HTML 内嵌 JavaScript 语法检查：通过（199938 字符，1 个 script 块）。
2. 后端测试 `cd backend && npm test`：9/9 通过，0 失败。
3. settings 测试 `node scripts/test-settings-panel.js`：15/15 通过。
4. 登录持久化测试 `node scripts/test-login-persistence.js`：9/9 通过。
5. 首页展示结构测试 `node scripts/test-home-structure.js`：10/10 通过（前一步遗留，本次未破坏）。
6. 新增玩法内容结构及兼容性测试 `node scripts/test-play-content.js`：9/9 通过，覆盖：
   - playManage 面板标题为“玩法内容”，旧标题“玩法管理”已清除
   - renderPlayEditor 保留全部 data-play-field 属性（key/icon/title/subtitle/image/images/text/points）
   - 每个玩法编辑区包含图标/标题/副标题/介绍文字/图片/玩法要点
   - 排序（上移/下移）、删除、保存入口齐全
   - “新增玩法”按钮存在且全局唯一（不再在每个卡片内重复）
   - 旧数据 description 字段兼容回退（item.text || item.description）
   - savePlayItem 仍按原字段读取（保存逻辑兼容）
   - 未误伤首页内容/建筑模板/玩家群的编辑函数
7. 运行中的本地服务（127.0.0.1:3000）HTTP 返回 200，已提供包含新结构、新标签与新按钮的页面（抽样计数全部命中）。

是否部署：否（仅本机 3000 端口本地验证，未上传服务器）。
后续注意事项：
- 真实浏览器验证（登录、刷新、保存、前台显示、玩法增删排序）交由总指挥复验；自动化结构测试与服务端响应抽样已通过。
- 旧玩法数据打开后可正常编辑；新增/上移/下移/删除/保存入口齐全，数据格式与保存逻辑不变。
- 前台“玩法”页展示不受本次改动影响。

---

### 2026-07-09 第二阶段第4小步：整理“玩家群入口”页面

时间：2026-07-09 16:16
操作人：WorkBuddy
操作类型：后台面板结构优化（TDD）、备份、验证
涉及文件：
- `erp14-server-showcase.html`（groupManage 模板）
- `scripts/test-group-entry.js`（新增测试脚本，先写失败再最小修改使其通过）
- `更新日志.md`

改动范围（仅 groupManage 面板，遵循 TDD：先写失败的 test-group-entry.js，再最小修改使其转绿）：
- 面板标题由“玩家群设置”改为“玩家群入口”，与左侧菜单按钮“玩家群入口”保持一致（与前面第2/3小步标题对齐菜单的做法一致）。
- 把全部字段按用途拆成两个清晰区域（不再堆在一个卡片里）：
  1. “加入入口”卡片：按钮文字（groupJoinText）、加入链接（groupJoinUrl）、加入申请说明（groupJoinApplication）
  2. “玩家群二维码”卡片：二维码图片（groupJoinQr，沿用原 renderManagedImageField 上传/删除/排序功能）
- 保存按钮 id="saveGroupSettings" 保持唯一，移至面板底部 `panel-save-bar`（与建筑模板/玩法内容一致）。
- **未新增任何当前系统没有的字段或功能**：QQ群（serverInfo.group / editGroup）属于“网站基础信息”设置面板，本页原本就不存在该字段，本次也未搬入；二维码功能本就存在，仅保持。
- **完全保留**原有数据结构、旧数据兼容、保存逻辑和前台显示：
  - 4 个字段 id（groupJoinText/groupJoinUrl/groupJoinApplication/groupJoinQr）一字未改；
  - `saveGroupSettings()` 函数未改动，只写入 joinText/joinUrl/joinApplication/joinQr 四个字段，不触碰 serverInfo.group 等“网站基础信息”其他字段；
  - 前台“加入玩家群”按钮（joinLink）仍读取 serverInfo.joinText/joinUrl/joinQr，展示不受影响。
- **未改动**：后端接口、数据库结构、首页内容（homeManage）、玩法内容（playManage）、建筑模板（templateManage）、活动管理（updateManage）、玩家建议、图片库、备份恢复、玩家登录、frontend 实验版。

备份位置：
- `C:\Users\Administrator\Desktop\SCUM用户网页\备份\玩家群入口整理-20260709-161643\`（含 erp14-server-showcase.html、更新日志.md）
- 备份仅放在桌面项目备份目录，未放到 O 盘或其他项目。

验证结果（每组测试均完整运行）：
1. HTML 内嵌 JavaScript 语法检查：通过（200197 字符，1 个 script 块）。
2. 后端测试 `npm test --prefix backend`：9/9 通过，0 失败。
3. `node scripts/test-settings-panel.js`：15/15 通过。
4. `node scripts/test-login-persistence.js`：9/9 通过。
5. `node scripts/test-home-structure.js`：10/10 通过（前序步骤遗留，本次未破坏）。
6. `node scripts/test-card-forms.js`：26/26 通过（前序步骤遗留，本次未破坏）。
7. `node scripts/test-play-content.js`：9/9 通过（前序步骤遗留，本次未破坏）。
8. 新增 `node scripts/test-group-entry.js`：7/7 通过（TDD 红→绿：改动前 2 项失败——标题非“玩家群入口”、仅 1 个区域；改动后全部转绿），覆盖：
   - groupManage 面板标题为“玩家群入口”（与菜单一致）
   - 内部按用途分成至少 2 个清晰区域（card pad 区块）
   - 原有 4 个字段 id 全部保留（joinText/Url/Application/Qr）
   - 保存按钮 id="saveGroupSettings" 全文件唯一
   - saveGroupSettings 只写入 join 字段，不覆盖 serverInfo.group
   - groupManage 未包含网站基础信息的 QQ群字段 editGroup
   - 未误伤首页内容/玩法内容/建筑模板/活动管理的编辑函数
9. 运行中的本地服务（127.0.0.1:3000）HTTP 返回 200，已提供包含新结构（加入入口 / 玩家群二维码 两个区域、新标题、唯一保存按钮）的页面（抽样计数全部命中：4 个字段 id 各 1、saveGroupSettings 1）。

是否部署：否（仅本机 3000 端口本地验证，未上传服务器）。
后续注意事项：
- 真实浏览器验证（登录、刷新、保存、前台“加入玩家群”显示、窄屏无溢出/重叠）交由总指挥复验；自动化结构测试与服务端响应抽样已通过。
- 按钮文字/链接/申请说明三者互不覆盖（各自独立字段，保存时分别写入）；旧数据打开后可正常编辑。
- 前台“加入玩家群”入口显示不受本次改动影响。
- QQ群信息仍在“网站基础信息”设置面板管理，不在本页。

---

## 阶段二第 5 小步：整理后台“首页展示 > 建筑模板”页面

日期：2026-07-09
执行：本项目执行工程师（按 AGENTS.md 与《后台布局优化实施步骤清单》执行，TDD 先红后绿）

目标：只整理 templateManage“建筑模板”后台页面，让每个模板编辑区更清楚，不新增业务功能。

具体改动（仅 `erp14-server-showcase.html`）：
- 面板标题 `建筑模板管理` → `建筑模板`（与左侧菜单按钮一致）。
- 每个模板卡片内按用途分成 4 个清晰区域（复用既有 `.panel-title` 子标题样式，不引入新 CSS）：
  - ① 基本信息：模板名称(title)、购买价格(price)、是否上架(onShelf)
  - ② 库存与购买：库存(stock)、限购(limit)、购买入口(buyUrl)
  - ③ 图片与说明：建筑模板图片(renderManagedImageField，沿用原上传/删除/排序)、模板介绍(description)、备注(note)
  - ④ 操作区：上移 / 下移 / 保存模板 / 删除
- 全部 9 个 `data-template-field`（title/price/onShelf/stock/limit/buyUrl/images/description/note）一字未改。
- `data-action` 名称全部保留：move-template-up / move-template-down / save-template / delete-template。
- “新增建筑模板”按钮（data-action="add-template"）仅在模板列表底部，全局唯一，未重复进每个卡片。
- `saveBuildingTemplate()` 函数未改动，仍按原字段读取并写入 buildingTemplates[index]（含 onShelf/stock/limit/buyUrl 不丢失、旧图片字段经 normalizeTemplateImages 兼容）。
- “新增建筑模板”初始数据结构（buildingTemplates.unshift）未改动。

完全保留：原有数据结构、旧数据兼容（normalizeTemplateImages 同时兼容 item.image 与 item.images）、上下架逻辑、前台展示（前台仍按原有规则读取 buildingTemplates 渲染）、新增/排序/删除的持久化行为、后端接口与数据库。

未改动：首页内容（homeManage）、玩法内容（playManage）、玩家群入口（groupManage）、活动管理（updateManage）、玩家建议、图片库、备份恢复、玩家登录、frontend 实验版。

备份位置：
- `C:\Users\Administrator\Desktop\SCUM用户网页\备份\建筑模板整理-20260709-204732\`（含 erp14-server-showcase.html、更新日志.md）
- 备份仅放在桌面项目备份目录，未放到 O 盘或其他项目。

验证结果（每组测试均完整运行）：
1. HTML 内嵌 JavaScript 语法检查：通过（200610 字符，1 个 script 块）。
2. 后端测试 `npm test --prefix backend`：9/9 通过，0 失败。
3. `node scripts/test-settings-panel.js`：15/15 通过。
4. `node scripts/test-login-persistence.js`：9/9 通过。
5. `node scripts/test-home-structure.js`：10/10 通过（前序步骤遗留，本次未破坏）。
6. `node scripts/test-card-forms.js`：26/26 通过（前序步骤遗留，本次未破坏）。
7. `node scripts/test-play-content.js`：9/9 通过（前序步骤遗留，本次未破坏）。
8. `node scripts/test-group-entry.js`：7/7 通过（前序步骤遗留，本次未破坏）。
9. 新增 `node scripts/test-template-content.js`：9/9 通过（TDD 红→绿：改动前 2 项失败——标题非“建筑模板”、仅 1 个区域；改动后全部转绿），覆盖：
   - 面板标题为“建筑模板”（与菜单一致）
   - 9 个现有字段的 data-template-field 全部保留
   - 旧图片字段兼容（normalizeTemplateImages 保留）
   - 新增建筑模板按钮全局唯一（data-action="add-template" 全文件唯一）
   - 上移/下移/保存/删除入口齐全
   - saveBuildingTemplate 仍读取原字段
   - onShelf/stock/limit/buyUrl 字段不丢失
   - 模板编辑器含 4 个清晰区域（基本信息/库存与购买/图片与说明/操作区）
   - 未误伤其他后台页面与函数（renderPlayEditor/renderTemplateEditor/saveGroupSettings/saveHomeHeroImages/saveHomeHero/homeManage/playManage/groupManage 均存在）
10. 运行中的本地服务（127.0.0.1:3000）HTTP 返回 200，已提供包含新结构（4 个区域、新标题、唯一新增按钮、4 类 data-action 与 9 个 data-template-field）的页面（抽样计数全部命中）。

是否部署：否（仅本机 3000 端口本地验证，未上传服务器）。
后续注意事项：
- 真实浏览器验证（登录、刷新、保存、模板新增/排序/删除、图片上传、前台模板展示、窄屏无溢出/重叠）交由总指挥复验；自动化结构测试与服务端响应抽样已通过。
- 旧建筑模板数据打开后可正常显示与编辑；上架/价格/库存/限购/购买链接不会丢失（字段与保存逻辑未改）。
- 前台只按原有规则展示建筑模板，不受本次改动影响。
- 已知提示：`erp14-server-showcase.html` 当前为 LF 行尾（本会话多步编辑经 Edit 工具统一），与已提交的 CRLF 版本相比 git diff 行数较大，属行尾规范化产物，非内容损坏；功能与测试均已验证无误。

---

## 阶段三第 1 小步：整理后台“活动管理 > 长期 / 限时活动”页面

日期：2026-07-09
执行：本项目执行工程师（按 AGENTS.md 与《后台布局优化实施步骤清单》执行，TDD 先红后绿）

目标：只整理 updateManage 活动管理界面，将“每周活动管理”统一为“长期 / 限时活动”，按 fixed（长期固定活动）在前、signup（限时报名活动）在后的顺序展示两个独立区域，不新增业务功能、不改变数据值。

具体改动（仅 `erp14-server-showcase.html`）：
- 面板标题 `每周活动管理` → `长期 / 限时活动`（与左侧菜单按钮文案一致）。
- 两个区域顺序对调：原“临时报名活动(signup)在前、长期固定活动(fixed)在后”改为“长期固定活动(fixed)在前、限时报名活动(signup)在后”。
- 区域标题与新增按钮文案 `临时` → `限时`：
  - 区域标题：临时报名活动（N 个） → 限时报名活动（N 个）
  - 新增按钮：新增临时报名活动 → 新增限时报名活动
  - 每个区域保留自己唯一的新增按钮（data-action="add-update" data-type 分别为 fixed / signup），互不合并。
- 长期固定活动区域强调色：原 `var(--primary)` 在本文件未定义（废弃变量、样式失效），改用已定义的 `var(--brand)`；限时报名活动区域保留已生效的 `var(--amber)`，两区视觉区分保持。
- renderUpdateEditor 内“活动类型”下拉的显示文本 `临时报名活动` → `限时报名活动`（`<option value="signup">` 的值不变，数据值 fixed/signup 不受影响）。
- renderUpdateEditor 内部区域结构（基本信息 / 活动内容 / 报名设置[仅 signup] / 结果排名 / 操作区）及全部 data-update-field、data-update-check、data-action、排名行字段、rewardDate、复制新一期/结束活动/导入报名结果按钮均未改动。

完全保留：原有数据结构、activityType 的 fixed/signup 数据值、全部现有字段与行为（标题/类型/状态/发布状态/开放时间/奖励/内容规则/备注/报名截止/活动结束/允许报名/报名名单/手动报名/排名/玩家名/成绩/奖励/发奖日期/复制新一期/结束活动/导入报名结果/新增/上移/下移/保存/删除）、saveUpdateItem / inferActivityType / getUpdateId 与报名后端同步逻辑、前台首页报名区与“每周活动”页面展示、后端接口与数据库。

未改动：首页内容（homeManage）、玩法内容（playManage）、玩家群入口（groupManage）、建筑模板（templateManage）、玩家建议、图片库、备份恢复、玩家登录、frontend 实验版。

备份位置：
- `C:\Users\Administrator\Desktop\SCUM用户网页\备份\活动管理整理-20260709-212917\`（含 erp14-server-showcase.html、更新日志.md）
- 备份仅放在桌面项目备份目录，未放到 O 盘或其他项目。

验证结果（每组测试均完整运行）：
1. HTML 内嵌 JavaScript 语法检查：通过（200602 字符，1 个 script 块）。
2. 后端测试 `npm test --prefix backend`：9/9 通过，0 失败。
3. `node scripts/test-settings-panel.js`：15/15 通过。
4. `node scripts/test-login-persistence.js`：9/9 通过。
5. `node scripts/test-home-structure.js`：10/10 通过（前序步骤遗留，本次未破坏）。
6. `node scripts/test-card-forms.js`：26/26 通过（前序步骤遗留，本次未破坏）。
7. `node scripts/test-play-content.js`：9/9 通过（前序步骤遗留，本次未破坏）。
8. `node scripts/test-group-entry.js`：7/7 通过（前序步骤遗留，本次未破坏）。
9. `node scripts/test-template-content.js`：9/9 通过（前序步骤遗留，本次未破坏）。
10. 新增 `node scripts/test-activity-content.js`：10/10 通过（TDD 红→绿：改动前 3 项失败——标题非“长期 / 限时活动”、限时区域不存在、新增限时按钮缺失；改动后全部转绿），覆盖：
   - 页面标题为“长期 / 限时活动”（且不再含“每周活动管理”）
   - 长期固定活动(fixed)区域排在限时报名活动(signup)区域前面
   - 两类新增按钮各自唯一且文案为“新增长期固定活动”/“新增限时报名活动”
   - fixed、signup 数据值保持不变（data-type 与 inferActivityType 判定保留）
   - 原有活动字段全部保留（title/schedule/reward/rules/notes/signupDeadline/eventEndAt/status/version/rewardDate/activityType-select + published/signupEnabled 复选框）
   - 报名设置只在 signup 类型显示（被 `activityType === 'signup'` 条件包裹）
   - 排名和发奖字段保留（data-results-rank/player/score/reward、rewardDate、add-result-row、import-signups-to-results）
   - 保存/排序(上移下移)/复制/结束/删除入口齐全（save-update/move-update-up/down/duplicate-update/end-event/delete-update）
   - saveUpdateItem 原有读取与后端同步逻辑未损坏（仍读 data-update-field、写入 activityType、向后端同步 type）
   - 未误伤其他后台模块（groupManage/templateManage/playManage/homeManage 及对应 save/render 函数均存在）
11. 运行中的本地服务（127.0.0.1:3000）HTTP 返回 200，已提供包含新结构（标题“长期 / 限时活动”、fixed 区在前 signup 区在后、两个唯一新增按钮、旧标题/旧按钮计数均为 0）的页面（抽样计数全部命中；顺序校验：长期固定活动出现在第 5422 行、限时报名活动在第 5429 行）。

是否部署：否（仅本机 3000 端口本地验证，未上传服务器）。
后续注意事项：
- 真实浏览器验证（登录、刷新、长期/限时活动新增/排序/复制/结束/删除、报名名单与结果排名、前台首页报名区与“每周活动”页面显示、窄屏无溢出/重叠）交由总指挥复验；自动化结构测试与服务端响应抽样已通过。
- 两类活动原有数据打开后可正常显示与编辑；fixed/signup 数据值、报名后端同步逻辑均不变。
- 前台首页报名区与“每周活动”页面展示不受本次改动影响（仅后台区域命名与顺序调整）。
- 已知提示：同上，`erp14-server-showcase.html` 当前为 LF 行尾（本会话多步编辑经 Edit 工具统一），与已提交的 CRLF 版本相比 git diff 行数较大，属行尾规范化产物，非内容损坏；功能与测试均已验证无误。

---

## 阶段三第 1 小步 · 返工：前台“每周活动”窄屏横向溢出修复

目标：仅修复前台“每周活动”页面在窄屏（375px）下的横向溢出，不改动活动数据、渲染函数、后端、其他页面；桌面布局不得退化。

问题证据（返工前实测）：375px 下 `document.scrollWidth > clientWidth`；`#events .update-list` 宽约 343px，但 `.update-card` 被撑宽至约 718px；结果排名区域（`.activity-results` 表格）是主要撑宽来源。

具体改动（仅 CSS，全部限定在 `#events` 作用域，避免污染后台编辑面板）：
- 在 `.activity-results-footer` 之后新增一段窄屏防溢出规则（约 11 行）：
  - `#events .update-list, #events .update-card { min-width: 0; }` —— 修复 grid 子项不收缩的默认行为，使卡片可在网格内收缩。
  - `#events .update-card { width:100%; max-width:100%; }` —— 约束卡片不超过容器。
  - `#events .update-card h3, p, .update-card-fields { overflow-wrap:break-word; word-break:break-word; min-width:0; }` —— 长文本/长单词正常换行。
  - `#events .update-card-fields { grid-template-columns: minmax(0,1fr); }` —— 字段网格单列且不超宽。
  - `#events .activity-results { overflow-x:auto; -webkit-overflow-scrolling:touch; }` —— 结果排名区域超出时仅在内部横向滚动，不再撑宽整页。
  - `#events .activity-results-table { width:100%; table-layout:fixed; }` 及 `th, td { overflow-wrap:break-word; word-break:break-word; }` —— 表格列宽固定、内容换行，避免被长文本顶宽。

完全保留：活动数据结构、activityType 的 fixed/signup 数据值、renderUpdateEditor / renderResultsTable / saveUpdateItem / inferActivityType / getUpdateId 及报名后端同步逻辑、后台“长期 / 限时活动”整理结果、首页/玩法/玩家群/模板/建议/登录/frontend 实验版、后端接口与数据库。桌面布局不受影响（新增规则仅增强窄屏韧性；`#events` 在移动端本就是单列）。

未改动：后台管理界面、活动数据、渲染函数、后端、其他页面、数据库。

新增专项检查：`scripts/test-activity-overflow.js`（静态 CSS 防溢出专项，7/7 通过）。

备份位置：
- `C:\Users\Administrator\Desktop\SCUM用户网页\备份\活动窄屏溢出修复-20260709-222543\`（含 erp14-server-showcase.html、更新日志.md）
- 备份仅放在桌面项目备份目录，未放到 O 盘或其他项目。

验证结果（每组测试均完整运行）：
1. HTML 内嵌 JavaScript 语法检查：通过（200602 字符，1 个 script 块）。
2. 后端测试 `npm test --prefix backend`：9/9 通过，0 失败。
3. `node scripts/test-settings-panel.js`：15/15 通过。
4. `node scripts/test-login-persistence.js`：9/9 通过。
5. `node scripts/test-home-structure.js`：10/10 通过。
6. `node scripts/test-card-forms.js`：26/26 通过。
7. `node scripts/test-play-content.js`：9/9 通过。
8. `node scripts/test-group-entry.js`：7/7 通过。
9. `node scripts/test-template-content.js`：9/9 通过。
10. `node scripts/test-activity-content.js`：10/10 通过（前台/后台整理结果未被破坏）。
11. 新增 `node scripts/test-activity-overflow.js`：7/7 通过，覆盖：
   - `#events .update-card` 含 `min-width:0`
   - 活动标题/段落/字段含 `overflow-wrap:break-word`
   - `.activity-results` 含 `overflow-x:auto`
   - `.activity-results-table` 含 `table-layout:fixed` 且 th/td 含 break-word
   - 规则限定在 `#events`（未污染 `.card.pad` / `.results-editor` 等后台样式）
   - `renderResultsTable` 函数与 `#events`/`#updateList` 结构完整
   - `saveUpdateItem` / `inferActivityType` 未改动（本次仅改 CSS）
12. 真实浏览器验证（无头 Chrome + CDP，`Emulation.setDeviceMetricsOverride` 375px mobile）：
   - 探针页（最坏情形：超宽结果表格）：`{"vw":375,"docScroll":375,"docClient":375,"docOverflow":false}` ✅
   - 线上已服务页面 `http://127.0.0.1:3000/`：`{"vw":375,"docScroll":375,"docClient":375,"docOverflow":false,"cards":4,"results":1}` ✅
   - 确认 `document.documentElement.scrollWidth === document.documentElement.clientWidth === 375`，无横向溢出。
13. 与返工前备份 `diff` 比对：确认唯一内容变更为上述约 11 行 `#events` 限定防溢出 CSS 块，无任何其他代码/数据改动。

是否部署：否（仅本机 3000 端口本地验证，未上传服务器）。
后续注意事项：
- 真实浏览器交互复验（登录、刷新、长期/限时活动新增/排序/复制/结束/删除、报名名单与结果排名、前台“每周活动”窄屏显示、其他窄屏机型 320/414px）交由总指挥复验；自动化结构测试、静态防溢出专项检查与 375px 真实浏览器无溢出验证均已通过。
- 桌面端（≥768px）布局未退化，新增 CSS 仅在窄屏生效。
- 已知提示：同上，`erp14-server-showcase.html` 当前为 LF 行尾，与已提交 CRLF 版本相比 git diff 行数较大，属行尾规范化产物，非内容损坏。

---

## 阶段三第 1 小步 · 返工（二）：修复“长期 / 限时活动”真实浏览器数据重复问题

目标：仅修复活动数据加载/合并逻辑，使后台“长期 / 限时活动”以 /api/events（访客）与 /api/admin/events（管理员）返回的 {code,data} 中 data 数组为权威来源，不再与旧 site-config.json 的 updates 重复合并，消除“基地评选活动”重复、限时报名活动变 3 个的问题。

问题根因（已用真实接口核实）：
- /api/events 仅返回 4 条活动（周末空投争夺开启[signup]、新手保护范围调整[fixed]、交易区展示页整理[fixed]、基地评选活动[signup]），即 长期固定(2)+限时报名(2)，无重复。
- /api/admin/config 当前已无 updates 键（DB config 表未存旧 updates）。
- 真实浏览器重复并非来自后端配置，而是：管理员路径 loadFullBackendConfig 此前**从不拉取活动接口**，活动列表只由“默认 updates(4) + localStorage 陈旧 updates + config.updates”决定；测试/保存过程中 localStorage 累积出含重复“基地评选活动”的 5 条，刷新后管理员面板直接渲染这 5 条（2 固定 + 3 限时，基地评选活动重复），导致所见 bug。

具体改动（仅 erp14-server-showcase.html，活动加载逻辑）：
- 新增纯函数 `mapEventsToUpdates(events)`：将后端事件（type/content/signup_deadline/event_end_at/results/reward_date/published 等）映射为前台统一活动模型（activityType/text/signupDeadline/eventEndAt/…），为活动列表权威来源。
- 移除 applyPublicBackendConfig 内“`if (Array.isArray(config.updates))` 按索引合并旧 updates”的代码块（重复/错位根因），并加注释说明活动来源不再合并旧 site-config updates。
- loadPublicBackendConfig（访客）：以 /api/events 返回的 data 数组为权威，覆盖 updates（fetchWithFallback 已自动解开 .data，并对“未解开”格式兼容）；仅当接口确实返回活动时才覆盖，后端不可用时回退到默认 updates（4 条）。管理员已登录时跳过，避免与后台全量加载并发覆盖。
- loadFullBackendConfig（管理员）：新增并行拉取 /api/admin/events（GET 返回全部活动，含未发布），以其 data 覆盖 updates（同样仅在有数据时覆盖）；该路径在 applyFullBackendConfig 之前执行，确保后台“长期 / 限时活动”以权威数据渲染，不被陈旧 localStorage 污染。

完全保留：活动数据结构、activityType 的 fixed/signup 数据值、renderUpdateEditor / saveUpdateItem / inferActivityType / getUpdateId 与报名后端同步逻辑、前台“每周活动”展示、其他后台页面、后端接口与数据库、桌面布局。新增/排序/复制/结束/删除/报名名单/结果排名功能均不受影响（saveUpdateItem 仍按原字段映射 POST/PUT /api/admin/events）。

未改动：其他后台页面、活动数据内容、后端、数据库、窄屏防溢出 CSS（上一返工保留）。

新增专项检查：`scripts/test-activity-merge.js`（10/10 通过），覆盖：
- 定义 mapEventsToUpdates；已移除旧的 config.updates 按索引合并逻辑；loadFullBackendConfig 拉取 /api/admin/events；loadPublicBackendConfig 以 /api/events 的 data 为权威且排除已登录管理员；代码注释明确不再合并旧 site-config updates。
- 行为模拟：提取真实 mapEventsToUpdates，模拟 /api/events 返回 {code:200,data:[4 条]} 时，覆盖后活动总数=4、基地评选活动仅 1 次、长期固定(2)+限时报名(2)、关键字段(id/text/signupDeadline/eventEndAt/published/signupEnabled)正确映射；后端无活动时回退旧默认 updates(4 条)。证明不与旧 updates 重复合并。

备份位置：
- `C:\Users\Administrator\Desktop\SCUM用户网页\备份\活动数据重复修复-20260709-230621\`（含 erp14-server-showcase.html、更新日志.md）
- 备份仅放在桌面项目备份目录，未放到 O 盘或其他项目。

验证结果（每组测试均完整运行）：
1. HTML 内嵌 JavaScript 语法检查：通过（203116 字符，1 个 script 块）。
2. 后端测试 `npm test --prefix backend`：9/9 通过，0 失败。
3. `node scripts/test-settings-panel.js`：15/15 通过。
4. `node scripts/test-login-persistence.js`：9/9 通过。
5. `node scripts/test-home-structure.js`：10/10 通过。
6. `node scripts/test-card-forms.js`：26/26 通过。
7. `node scripts/test-play-content.js`：9/9 通过。
8. `node scripts/test-group-entry.js`：7/7 通过。
9. `node scripts/test-template-content.js`：9/9 通过。
10. `node scripts/test-activity-content.js`：10/10 通过（前台/后台整理结果未被破坏）。
11. `node scripts/test-activity-overflow.js`：7/7 通过（窄屏防溢出未被破坏）。
12. 新增 `node scripts/test-activity-merge.js`：10/10 通过（见上）。
13. 真实浏览器验证（无头 Chrome + CDP，强制刷新）：
   - 前台“每周活动”：update-card 数量=4，标题为[周末空投争夺开启, 新手保护范围调整, 交易区展示页整理, 基地评选活动]。
   - 后台“长期 / 限时活动”（管理员登录，并模拟陈旧 localStorage 含重复基地评选活动 5 条）：长期固定活动（2 个）、限时报名活动（2 个）、update-card 数量=4、卡片标题中“基地评选活动”出现次数=1（无重复）。RESULT: PASS。
   - 即：即便 localStorage 残留重复数据，后台仍以 /api/admin/events 权威覆盖，稳定显示 2+2，不复现原 bug。

是否部署：否（仅本机 3000 端口本地验证，未上传服务器）。
后续注意事项：
- 真实浏览器交互复验（新增/排序/复制/结束/删除/报名名单/结果排名、未发布活动在管理员端可见、前台窄屏、其他窄屏机型 320/414px）交由总指挥复验；自动化结构测试、数据重复专项检查与真实浏览器前后台验证均已通过。
- 活动列表现以后端 events 表为唯一权威来源；如需新增活动，走后台“新增长期固定活动/新增限时报名活动”→ 保存即写入 DB，刷新后从接口读取，不再受旧 site-config updates 影响。
- 已知提示：同上，`erp14-server-showcase.html` 当前为 LF 行尾，与已提交 CRLF 版本相比 git diff 行数较大，属行尾规范化产物，非内容损坏。

---

## 阶段三第 1 小步 · 返工（三）：修复活动删除没有同步 DB 的问题 + 清理复验残留测试数据

**修改时间**：2026-07-09 23:55 — 2026-07-10 00:16
**备份目录**：`备份\活动删除同步DB修复-20260709-235505\`
**修改文件**：
- `erp14-server-showcase.html` — 仅修改 delete-update 处理器（DELETE 同步 DB + 成功后重新拉取 + 失败提示不假装成功）+ 强化 add-result-row 处理器（兜底查找 tbody + 自动展开结果排名区域）
- `scripts/test-activity-delete.js` — 新增（删除同步 DB 专项测试，10/10 通过）
- `更新日志.md` — 追加本次记录

**具体改动**：
1. **delete-update 处理器**（已保存在后端的活动 → 调用 `DELETE /api/admin/events/${idNum}` → 成功后 `await loadFullBackendConfig()` 以权威接口重新拉取活动列表并渲染 → 失败时 `showToast(msg,'error')` 并 `return`，不假装删除成功；未保存的本地草稿活动仍走 `updates.splice` 前端删除）。
2. **add-result-row 处理器**：加兜底（若按 id 找不到 `resultsBody-${index}`，从当前编辑器 `<details>` 内定位结果表格）；自动展开结果排名 `<details>` 以确保新增行对用户可见。
3. **清理复验残留**：通过 API 删除 `id=5`（总指挥复验临时活动-1783611906974），清理由本次复验产生的测试结果数据（`id=2` results 恢复为空）。

**测试结果**：
- HTML 内嵌 JS 语法：✅ 204,655 字符
- 后端：✅ 9/9
- 全部前端测试（含新 activity-delete）：✅ 全部通过：
  - settings-panel 15/15, login-persistence 9/9, home-structure 10/10, card-forms 26/26, play-content 9/9, group-entry 7/7, template-content 9/9
  - activity-content 10/10, activity-overflow 7/7, activity-merge 10/10, **activity-delete 10/10**

**真实浏览器验证（无头 Chrome + CDP）**：
1. 删除同步：POST 临时活动 → 前台 Delete → 确认弹窗确认 → `inPanel: false`, `inApi: false`, `apiCount: 4` ✅
2. 添加排名行：`before:0 → after:1`, `opened:true`（行新增且结果排名区域自动展开） ✅
3. id=5 残留已清理：`id5present: false` ✅
4. 长期固定(2) + 限时报名(2) 稳定显示 ✅
5. 前台"每周活动"卡片数 = 4 ✅

**是否部署**：否（仅本机 3000 端口本地验证，未上传服务器）。
**已知提示**：同上，`erp14-server-showcase.html` 当前为 LF 行尾，与已提交 CRLF 版本相比 git diff 行数较大，属行尾规范化产物，非内容损坏。

---

## 阶段三第 1 小步 · 返工（四）：修复「结果排名添加行」的真实浏览器交互

**问题证据**：总指挥在真实浏览器复验发现，「基地评选活动」结果排名 `<details>` 默认折叠（closed），「+ 添加排名行」按钮在折叠区域内无法被用户点击命中（真实点击事件不传递到 collapsed details 内部），导致 `rows after = 0`、`opened = false`。手动展开结果排名后再点击则正常。

**修改时间**：2026-07-10 18:06 — 18:12
**备份目录**：`备份\添加排名行按钮移出details-20260710-180659\`
**修改文件**：仅 `erp14-server-showcase.html`
1. 渲染模板（`renderUpdateEditor`）：将「+ 添加排名行」按钮与「从报名名单导入」按钮从 `<details class="editor-section">`（结果排名）内部移到外部，按钮始终在 DOM 中可见可点。
2. `import-signups-to-results` 处理器：导入后自动展开结果排名 `<details>`。
3. `add-result-row` 处理器：去掉不再需要的兜底 fallback；用 `getElementById` 直接定位 tbody（即使 details 折叠，tbody 仍在 DOM 中）；点击后自动展开。

**测试结果**：
- HTML 语法：✅ 204,472 字符
- 后端：✅ 9/9
- 全部前端测试（activity-content 10/10 + overflow 7/7 + merge 10/10 + delete 10/10 + 其余 7 项）：✅ 全部通过

**真实浏览器验证（无头 Chrome + CDP）**：
1. 基地评选活动，结果排名默认折叠（`detailsWasOpen: false`）
2. 直接点击「+ 添加排名行」→ `before: 0 → after: 1` ✅
3. 结果排名自动展开：`detailsNowOpen: true` ✅
4. 按钮始终可点击：`btnExists: true` ✅
5. 长期固定(2) + 限时报名(2) + 前台每周活动 4 卡片：✅

**是否部署**：否

---

## 阶段三第 2 小步：整理后台「建议处理」页面

**修改时间**：2026-07-10 18:54 — 19:10
**备份目录**：`备份\阶段三-第2小步-建议处理修改前备份-20260710-185423\`
**修改文件**：
- `erp14-server-showcase.html` — 仅改写 `renderRequestManagePanel()` 函数（移除筛选侧栏，改为按状态分三区展示）
- `scripts/test-request-manage.js` — 新增（19/19 测试通过）
- `更新日志.md` — 追加本次记录

**具体改动（仅建议处理面板渲染）**：
1. 面板标题从「玩家建议管理」改为「建议处理」。
2. 移除左侧筛选侧栏（全部/待讨论/已计划/已完成/已拒绝筛选 + 计数卡片）。
3. 建议按状态分成三个独立区域，顺序固定：
   - **待讨论 / 新提交建议**（`pending` + `planned` 状态）
   - **已完成**（`done` 状态）
   - **已拒绝**（`rejected` 状态）
4. 每条建议卡片完全保留：标题、玩家名、分类、建议内容、图片、同意数/否定数、状态选择器、管理员回复、拒绝原因、查看/上移/下移/删除按钮。
5. `saveRequestChanges` / `delete-request` / `renderRequestAdminCard` / 前台 `renderRequests` / 前台提交流程：**不做任何修改**。

**测试结果**：
- HTML 内嵌 JS 语法：✅ 203,694 字符
- 后端：✅ 9/9
- 全部 12 个前端测试：✅ 全部通过
  - settings-panel 15/15, login-persistence 9/9, home-structure 10/10, card-forms PASS
  - play-content 9/9, group-entry 7/7, template-content 9/9
  - activity-content 10/10, activity-overflow 7/7, activity-merge 10/10, activity-delete 10/10
  - **request-manage 19/19**（新增）

**是否部署**：否

### 补充同步：同步 frontend/js/main.js 的建议处理页面逻辑

**修改时间**：2026-07-10 19:39 — 19:42
**备份**：`备份\阶段三-第2小步-补充同步frontend-20260710-193902\`
**修改文件**：仅 `frontend/js/main.js`（替换 `renderRequestManagePanel()` 函数为 HTML 中已验收版本）
**未改**：`erp14-server-showcase.html`、后端、其他模块
**测试结果**：`main.js` 语法 ✅、backend ✅ 9/9、全部 12 个前端测试 ✅（含 request-manage 19/19）
**是否部署**：否

## 阶段三第 3 小步：整理后台「图片库」页面

**修改时间**：2026-07-10 20:10 — 20:30
**备份目录**：`备份\阶段三-第3小步-图片库整理修改前备份-20260710-201101\`
**修改文件**：
- `erp14-server-showcase.html` — 改写 `renderImageLibraryPanel()` + `loadImageLibrary()` 网格渲染
- `frontend/js/main.js` — 同步同上两个函数
- `scripts/test-image-library.js` — 新增（21/21 通过）

**具体改动（仅界面清晰度优化）**：
1. **存储概览**：从纯文字行改为带边框圆角的卡片，更醒目。
2. **上传区**：从单行按钮改为独立卡片，带标题「上传图片」、格式/大小说明、📤 图标按钮。
3. **图片卡片**：元信息增加文件大小显示（`· 123KB`）；「📋 复制地址」按钮改为品牌橙色边框，与「删除」按钮明显区分。
4. **`loadImageLibrary()`**：同步上述卡片模板改进（API 加载后同样显示大小 + 新按钮样式）。
5. **批量删除**：已有 `confirmAction` 确认弹窗，未改动。

**未改**：上传接口、保存路径、数据字段、后端接口、数据库、其他后台模块、前台展示。

**测试结果**：
- HTML 内嵌 JS 语法：✅ 204,475 字符
- `frontend/js/main.js` 语法：✅
- 后端：✅ 9/9
- 全部 13 个前端测试：✅ 全部通过
  - 原有 12 项（settings/login/home/card-forms/play/group/template/activity-content/overflow/merge/delete/request-manage）全部通过
  - **image-library 21/21**（新增）

**是否部署**：否

---

## 第四阶段第 1 小步：备份恢复安全加固

**修改时间**：2026-07-10 20:40 — 21:10
**备份目录**：`备份\第四阶段-第1小步-备份恢复安全加固修改前备份-20260710-204059\`
**修改文件**：
- `erp14-server-showcase.html` — 新增 `autoBackupForSafety()`、`promptConfirmAction()`；改写 `renderBackupPanel()`、`importSelectedModules()`、`resetSiteData()`
- `frontend/js/main.js` — 同步上述全部 5 个函数
- `scripts/test-backup-safety.js` — 新增（19/19 通过）

**具体改动**：
1. **页面标题**：从「备份与恢复」改为「备份恢复」，与左侧菜单一致。
2. **安全提示**：面板顶部新增黄色警告卡片，提示「普通代码更新不需要导入或重置数据，直接覆盖 HTML/JS 文件即可」。
3. **导入前自动备份**：`importSelectedModules()` 解析备份文件后、展示确认弹窗前，先调用 `autoBackupForSafety('导入')`——生成 JSON 下载文件 + 写入 localStorage。备份失败则操作停止。
4. **重置前自动备份**：`resetSiteData()` 第一步调用 `autoBackupForSafety('重置')`，备份成功后才继续。
5. **导入确认弹窗明确列出模块**：弹窗文案改为「即将导入以下模块，当前数据将被覆盖：\n网站配置\n玩家建议\n活动数据\n报名名单\n图片列表」，逐项列出勾选模块。
6. **重置二次确认**：
   - 第 1 步：标准 `confirmAction` 弹窗，说明已自动备份。
   - 第 2 步：新增 `promptConfirmAction()`——要求输入「确认重置」文字才能执行，输入正确前确认按钮禁用。
7. **`autoBackupForSafety(label)`**：新函数，生成当前数据快照 + 自动下载 JSON + 写入 localStorage，失败返回 false 阻止操作。
8. **`promptConfirmAction(title, message, requiredText)`**：新函数，复用现有 `#confirmModal`，动态插入文本输入框，输入内容匹配后才启用确认按钮。

**测试结果**：
- HTML 内嵌 JS 语法：✅ 209,127 字符
- `frontend/js/main.js` 语法：✅
- 后端：✅ 9/9
- 全部 14 个前端测试：✅ 全部通过
  - 原有 13 项全部通过
  - **backup-safety 19/19**（新增）

**是否部署**：否

---

## 第四阶段第 2 小步：操作日志轻量优化

**修改时间**：2026-07-10 22:27 — 22:45
**备份目录**：`备份\第四阶段-第2小步-操作日志轻量优化修改前备份-20260710-222751\`
**修改文件**：
- `erp14-server-showcase.html` — 新增 `isHighRiskAction()` + CSS `.log-row-high-risk`；改写 `logs:` 面板模板 + `loadLogs()` 行渲染
- `frontend/js/main.js` — 同步同上
- `scripts/test-operation-logs.js` — 新增（32/32 通过）

**具体改动**：
1. **面板模板**：标题「操作日志」；新增高风险说明卡（红色左边框 + tag danger「高风险」）；筛选区增加「导入数据」「批量删除」「重置」三个操作类型选项；表格加一列高风险标记列。
2. **高风险检测**：新增 `isHighRiskAction(action, detail)` 函数，检查是否含"删除"、"导入"、"重置"、"批量删除"关键词。
3. **行渲染**：高风险行添加 CSS 类 `.log-row-high-risk`（红色左边框 + 浅红背景）+ 首列显示 `<span class="tag danger">高风险</span>` 标签。
4. **CSS**：新增 `.log-row-high-risk td { border-left:4px solid var(--danger); background: color-mix(...) }`。
5. **未改动**：后端接口、数据库、日志保存逻辑、其他后台模块、前台展示。

**测试结果**：
- HTML 内嵌 JS 语法：✅ 210,168 字符
- `frontend/js/main.js` 语法：✅
- 后端：✅ 9/9
- 全部 15 个前端测试：✅ 全部通过
  - 原有 14 项全部通过
  - **operation-logs 32/32**（新增）

## 代码结构维护第 1 小步：同步检查与代码地图更新

**修改时间**：2026-07-10 23:58 — 00:10
**备份目录**：`备份\代码结构维护-同步检查与代码地图更新前备份-20260710-235844\`
**修改文件**：
- `scripts/check-frontend-sync.js` — 新增（80 项检查，74 通过 / 6 项为动态生成 DOM id 的预期差异）
- `代码地图.md` — 全面改写：更新行号、添加同步检查脚本说明、修改建议流程、新增备份与部署说明
- `更新日志.md` — 追加本次记录

**具体改动**：
1. **新增同步检查脚本** `scripts/check-frontend-sync.js`：
   - 检查文件存在性、CSS/JS 行数近似度
   - 检查 28 个关键函数在 HTML 和 main.js 中同时存在
   - 检查 13 个面板入口、DOM id、CSS 类名一致性
   - 仅输出报告，不修改文件
2. **更新代码地图.md**：
   - 修正已过期的行号（如 `renderRequestManagePanel` 从 5947→6050）
   - 明确主生产文件为 `erp14-server-showcase.html`
   - 明确 `frontend/` 为拆分源码镜像
   - 添加「修改代码的建议流程」（改主源→同步 frontend→跑同步检查→跑测试→记录日志）
   - 添加同步检查脚本使用方法
   - 添加备份与部署说明

**同步检查结果**：74/80 通过（6 项差异为：
- `renderPlayItems` 函数名不准确——实际渲染函数命名不同；
- `searchLogs`/`resetLogs`/`logList`/`selectAllImages`/`totalImageCount` 为 JS 动态生成的 DOM id，不在静态 index.html 中）
实际核心文件同步良好，无实质不同步问题。

**验证结果**：
- 同步检查脚本：✅ 正常运行并输出报告
- HTML 内嵌 JS 语法：✅ 210,168 字符
- `frontend/js/main.js` 语法：✅
- 后端：✅ 9/9
- 全部 15 个专项测试：✅ 全部通过

### 补充返修：同步检查脚本的预期差异与真实失败分离

**修改时间**：2026-07-11 00:05 — 00:15
**修改文件**：
- `scripts/check-frontend-sync.js` — 重构检查分类
- `代码地图.md` — 更新示例输出和注意说明
- `更新日志.md` — 追加本次记录

**具体改动**：
1. **脚本分类重构**：`renderPlayItems` → `renderPlay`（真实函数名）；5 个 JS 动态 DOM id（`searchLogs`/`resetLogs`/`logList`/`selectAllImages`/`totalImageCount`）从 `✗ 失败` 改为 `⚠ 预期提示`。
2. **退出码**：真实失败 > 0 时返回非 0，预期提示不影响退出码。
3. **代码地图.md**：示例输出更新为三段式（通过/预期提示/真实失败），注意说明改为 `⚠ 标记` 描述。
4. **核心逻辑不变**：不改 HTML、main.js、后端。

**同步检查结果**：75 通过 / 5 预期提示 / **0 真实失败** ✅ 退出码 0

**验证结果**：全部同前——HTML 语法 ✅、main.js ✅、backend 9/9 ✅、15 项专项测试全部通过 ✅

**是否部署**：否

---

## 图片库中文文件名修复（阶段三第 3 小步补充）

**修改时间**：2026-07-11 02:06 — 02:25
**备份目录**：`备份\图片库中文文件名修复前-20260711-020619\`
**修改文件**：
- `backend/server.js` — 新增 `safeOrigName(file)` 函数（latin1→UTF-8 转码），在所有 3 个图片上传入口中将 `req.file.originalname` 替换为 `safeOrigName(req.file)`
- `erp14-server-showcase.html` — `upload-library-image` 处理器：用 `file.name`（浏览器 File 对象原始文件名）替换合成名 `图片 日期-序号`
- `frontend/js/main.js` — 同步同上
- `scripts/test-image-library.js` — 新增 3 项检查（24/24 通过）

**修复内容**：
1. **根因**：multer 将 HTTP multipart Content-Disposition 的 filename 按 latin1 解码，中文字符被损坏。存入数据库的 `name` 字段已是乱码。
2. **后端**：`safeOrigName(file)` 将 `file.originalname` 从 latin1 重编码为 UTF-8 后再存库。
3. **前端**：上传立即展示时使用 `input.files[i].name`（浏览器原生 UTF-8 文件名），不再用 `图片 日期-序号` 合成名。刷新后后端返回正确 UTF-8 文件名。

**验证结果**：
- 后端测试：✅ 9/9
- 图片库专项测试：✅ 24/24（新增 3 项）
- HTML 语法：✅
- main.js 语法：✅
- 全部专项测试回归：✅（各模块均通过）

## 代码结构维护第 2 小步：建立生产页生成预检脚本

**修改时间**：2026-07-11 03:05 — 03:25
**备份目录**：`备份\代码结构维护-第2小步-建立生产页生成预检脚本前-20260711-030520\`
**修改文件**：
- `scripts/build-frontend-preview.js` — 新增（合并 frontend/ 源码为单页预览 HTML）
- `scripts/test-build-frontend-preview.js` — 新增（30/30 测试通过）
- `代码地图.md` — 新增「生产页预检脚本」说明章节
- `更新日志.md` — 追加本次记录

**脚本功能**：
- 读取 `frontend/index.html`、`frontend/css/main.css`、`frontend/js/main.js`
- 将 CSS 内嵌到 `<style>`、JS 内嵌到 `<script>`
- 输出 `dist/erp14-server-showcase.preview.html`
- 不覆盖 `erp14-server-showcase.html`，不修改 `frontend/` 任何文件

**验证结果**：
- 同步检查：75 通过 / 5 预期提示 / **0 真实失败** ✅
- `build-frontend-preview.js`：✅ 生成 329KB 预览文件
- `test-build-frontend-preview.js`：✅ 30/30 全部通过
- HTML 语法：✅ 210,389 字符
- `main.js` 语法：✅
- 后端：✅ 9/9
- 全部 15 个已有专项测试：✅ 全部通过

### 补充返修：强化 preview 构建专项测试

**修改时间**：2026-07-11 03:12 — 03:30
**修改文件**：仅 `scripts/test-build-frontend-preview.js`
**不改**：`build-frontend-preview.js`、`代码地图.md`、业务代码

**改动内容**：
1. **删除 `catch (e) {}`**：构建失败时会抛出异常，测试立即失败，不再吞错误。
2. **SHA256 校验**：构建前记录 `erp14-server-showcase.html` / `frontend/index.html` / `frontend/css/main.css` / `frontend/js/main.js` 的 SHA256；构建后重新计算并断言前后一致。
3. **preview 文件再生验证**：测试前先删除旧 preview 文件，构建后确认已重新生成。
4. 原有 30 项结构检查全部保留，新增 1 项再生验证 → **31/31 通过**。

**验证结果**：同步检查 ✅、build ✅、test 31/31 ✅、HTML ✅、main.js ✅、backend 9/9 ✅、全部 16 个专项测试 ✅

**是否部署**：否

---

## 代码结构维护第 3 小步：新增一键验证总脚本

**修改时间**：2026-07-11 03:17 — 03:35
**备份目录**：`备份\代码结构维护-第3小步-新增一键验证总脚本前-20260711-031759\`
**修改文件**：
- `scripts/verify-all.js` — 新增（一键验证总脚本）
- `scripts/test-verify-all.js` — 新增（14/14 测试通过）
- `代码地图.md` — 新增「一键验证总脚本」说明章节 + 修改建议流程
- `更新日志.md` — 追加本次记录

**verify-all.js 职责**：
按顺序运行 7 个阶段，任意阶段失败立即停止，输出失败阶段名和退出码。

| 步骤 | 内容 | 说明 |
|---|---|---|
| 1 | 同步检查 `check-frontend-sync.js` | 验证 HTML ↔ frontend/ 同步 |
| 2 | 生产页预检 `build-frontend-preview.js` | 合并 frontend/ → 单页预览 |
| 3 | preview 构建测试 `test-build-frontend-preview.js` | 含 SHA256 源文件校验 |
| 4 | HTML 内联 JS 语法检查 | `new Function(htmlScript)` |
| 5 | main.js 语法检查 | `new Function(mainJs)` |
| 6 | 后端测试 `npm test --prefix backend` | 9 项 |
| 7 | 全部 `test-*.js` 专项测试 | 跳过第 3 步已运行的 |

**验证结果**：
- `verify-all.js` 运行：✅ 全部验证通过（16 个测试套件）
- `test-verify-all.js`：✅ 14/14 全部通过
- `check-frontend-sync.js`：✅ 0 真实失败

## 补充文档：一键验证总脚本写入《操作手册.md》

**修改时间**：2026-07-11 03:23 — 03:28
**修改文件**：
- `操作手册.md` — 新增第 3 节「常用验证命令 / 一键验证总脚本」
- `更新日志.md` — 追加本次记录

**具体改动**：
在操作手册靠前位置（第 2 节「接手项目先做什么」之后）新增章节，写清楚：
- 命令：`node scripts/verify-all.js`
- 是什么（本地一键检查命令，不是部署）
- 什么时间运行（修改功能后 / 整理代码后 / 交付复验前）
- 通过标准（看到"全部验证通过"才算通过）
- 它不是什么（不上传服务器 / 不改数据库 / 不发布网站）

**验证**：`node scripts/verify-all.js` → ✅ 全部通过

## 代码结构维护阶段收尾：整理交付报告

**修改时间**：2026-07-11 03:27 — 03:35
**修改文件**：
- `代码结构维护阶段收尾报告.md` — 新增（本阶段完整收尾报告）
- `更新日志.md` — 追加本次记录

**报告内容**：
- 本阶段 3 大步 + 4 次补充的任务汇总
- 5 个新增脚本清单及用途
- 标准操作流程（备份→改动→日志→verify-all→复验→不部署）
- 文件关系说明（正式生产文件 vs 拆分源码 vs 预览文件）
- 后续小步方向建议（CSS 同步 / 窄屏收尾 / 代码定位优化 / 部署准备）

**验证**：`node scripts/verify-all.js` → ✅ 全部通过

## 部署前检查第 1 小步：部署信息盘点与风险清单

**修改时间**：2026-07-11 03:34 — 03:42
**备份目录**：`备份\部署前检查第1小步-部署信息盘点前-20260711-033415\`
**修改文件**：
- `部署前检查报告.md` — 新增（9 节完整部署前检查报告）
- `更新日志.md` — 追加本次记录

**报告内容**：
1. 当前本地项目状态（文件规模、目录结构）
2. 本次准备上线的内容范围（12+ 个模块改动汇总）
3. 不应该上传或部署的内容清单（备份/、dist/、node_modules/ 等）
4. 服务器部署前必须确认的问题清单（11 项）
5. 上线前必须备份的服务器内容（7 项建议）
6. 部署风险清单（8 项风险及缓解措施）
7. 回滚方案草案（4 种异常场景的恢复步骤）
8. 部署前本地验证结果（verify-all ✅ 全部通过）
9. 下一步建议（需总指挥确认后才能进入第 2 小步）

**验证**：`node scripts/verify-all.js` → ✅ 全部验证通过

**是否部署**：否

---

## 2026-07-11 记录

### 2026-07-11 新手入服引导卡 Task 1：增加结构测试

**修改时间**：2026-07-11 03:49 — 03:54
**备份目录**：`备份/test-home-structure.20260711-035449.js`
**修改文件**：仅 `scripts/test-home-structure.js`

**改动内容**：
- 在现有首页结构测试末尾新增 8 项新手入服引导卡结构断言：
  - 页面存在 `id="newPlayerGuideSection"`（新手入服引导区）
  - 页面存在 `id="guideServerIp"`（动态服务器 IP）
  - 页面存在 `id="guideGroupNumber"`（动态 QQ 群号）
  - 5 个 `data-guide-action` 按钮：`copy-ip`、`copy-group`、`read-rules`、`enter-server`、`contact-admin`
- 断言沿用现有测试风格（if/throw + TAP 格式），未引入 assert 模块

**验证结果**：
- `node scripts/test-home-structure.js`：**18 项测试，10 通过 / 8 失败**
- 新增 8 项全部因"新手入服引导结构尚不存在"而失败，符合预期
- 原有 10 项测试全部通过，未被破坏

**提交号**：`753eda2`
**提交信息**：`test: define new player guide structure`
**提交范围**：仅 `scripts/test-home-structure.js`，未混入其他已有改动

**是否部署**：否
**后续注意事项**：
- Task 1 完成，等待总指挥验收
- 未修改 frontend/、backend/、生产页，未实现任何前台功能
- 后续 Task 2 实现引导卡静态结构后，本测试应全部转绿

### 2026-07-11 新手入服引导卡 Task 1 修正：测试目标改为 frontend/index.html

**修改时间**：2026-07-11 03:58
**操作类型**：测试修正
**涉及文件**：`scripts/test-home-structure.js`
**备份位置**：无（上次备份可用）

**修正原因**：首次提交中 8 条新增断言检查的是 `erp14-server-showcase.html`（生产页），但 Task 2 只允许修改 `frontend/index.html`，因此第二阶段完成后测试仍不会通过。

**改动内容**：
- 新增 `frontendHtml` 变量，读取 `frontend/index.html`
- 将 8 条新手入服引导卡断言从 `html` 改为 `frontendHtml`
- 原有 10 条测试保持不变（继续检查 `erp14-server-showcase.html`）

**验证结果**：
- `node scripts/test-home-structure.js`：18 项，10 通过 / 8 失败 ✅
- 失败原因仍全部指向新手引导结构尚不存在 ✅

**提交号**：`43f7518`
**提交信息**：`test: target editable frontend guide`
**是否部署**：否