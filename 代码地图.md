# ERP14 前端代码地图

本文档用于快速定位 `erp14-server-showcase.html` 和拆分后的 `frontend/` 文件。

## 当前文件关系

| 文件 | 作用 | 说明 |
| --- | --- | --- |
| `erp14-server-showcase.html` | **主生产文件**（单文件） | 内嵌全部 CSS + HTML 主体 + JS。所有功能修改首先以本文件为准。 |
| `frontend/index.html` | 从主 HTML 拆分出的页面结构源码镜像 | 引用 `./css/main.css` 和 `./js/main.js`。 |
| `frontend/css/main.css` | 从 `<style>` 提取出的样式源码镜像 | 保持同步。 |
| `frontend/js/main.js` | 从 `<script>` 提取出的前端逻辑源码镜像 | 保持同步。 |
| `backend/server.js` | 后端 API 服务 | 负责登录、配置、活动、建议、图片、导入导出等接口。 |

> **重要**：生产实际使用的文件仍是 `erp14-server-showcase.html`（单文件），`frontend/` 仅供拆分开发和代码复用参考。

## 原始 HTML 大区间（erp14-server-showcase.html）

| 区间 | 内容 | 说明 |
| --- | --- | --- |
| `HTML:1-6` | HTML 头部 | 很少改。 |
| `HTML:7-4624` | 全部 CSS 样式（~4618 行） | 对应 `frontend/css/main.css`（~4600 行）。 |
| `HTML:4626-4936` | 页面主体 HTML：导航、前台区域、后台面板骨架、弹窗 | 对应 `frontend/index.html`（350 行）。 |
| `HTML:4953-9158` | 全部前端 JS（~4206 行） | 对应 `frontend/js/main.js`（~3909 行）。HTML 模板字符串含更多换行符。 |
| `HTML:9158+` | 收尾标签 | 基本不改。 |

## 首页功能

| 位置（HTML 行号） | 函数/区域 | 作用 |
| --- | --- | --- |
| `5621` | `renderHomeStats()` | 渲染首页数据卡。 |
| `5641` | `renderHomeFeatures()` | 渲染首页特色卡。 |
| `5667` | `renderHomeSuggestions()` | 渲染首页已完成建议。 |
| `5700` | `renderBuildingTemplates()` | 渲染建筑模板。 |
| `5722` | `renderServerRules()` | 渲染服务器规则。 |
| `7055` | `renderHero()` | 渲染首页主视觉区域。 |
| `7103` | `renderHeroCarousel()` | 渲染首页轮播图按钮和状态。 |
| `—` | `renderAll()` | 统一刷新所有前台展示。行号随版本变化，搜索即可。 |
| `—` | `renderPlay()` | 渲染玩法内容，搜索即可。 |

### 头图快捷操作（v2026-07-11 替换）

| 位置（frontend/index.html 行号） | ID/选择器 | 作用 |
| --- | --- | --- |
| `45` | `heroServerNo` | 服务器编号，普通文本展示。 |
| `53` | `heroServerIp` | IP 地址，点击复制 `serverInfo.ip`。 |
| `58` | `heroGroupLink` | QQ 群链接，打开 `serverInfo.joinUrl`。 |
| `64` | `heroGroup` | QQ 群号，普通文本展示。 |

| 位置（frontend/css/main.css 行号） | 选择器 | 作用 |
| --- | --- | --- |
| `474-490` | `.hero-info-action` | 胶囊样式（`min-height: 32px; padding: 0 11px; border-radius: 999px`）。 |
| `492-497` | `.hero-info-action:hover` | hover 背景变亮、边框变亮、`translateY(-1px)`。 |
| `499-501` | `.hero-info-action:active` | active 恢复 `translateY(0)`。 |
| `503-508` | `.hero-info-action:focus-visible` | 键盘 Tab 聚焦清晰轮廓。 |
| `3699-3702` | `.hero-info-action, a.hero-info-action` | 导航菜单高亮区保留胶囊样式。 |
| `4413` | `.hero-info-action`（@media ≤767px） | 移动端 `min-height: 44px`。 |
| `4422-4425` | `.hero-info-action`（@media ≤767px） | 移动端横向内边距 `8px`、字体 `12px`。 |
| `4446` | `[data-theme="dark"] .hero-info-action` | 暗色主题文字颜色。 |

| 位置（frontend/js/main.js 行号） | 函数/事件 | 作用 |
| --- | --- | --- |
| `738` | `copyHeroText()` | 兼容复制函数（`navigator.clipboard.writeText` + `execCommand('copy')` 降级）。 |
| `1999-2015` | `renderHero()` | 渲染头图信息，同步 `serverInfo.joinUrl` 到 `heroGroupLink`。 |
| `3929-3936` | `data-hero-action="copy-ip"` 事件绑定 | 点击复制 `serverInfo.ip`。 |

### 移动端头部触控按钮（v2026-07-15 优化）

| 位置（frontend/css/main.css 行号） | 规则 | 规格 |
| --- | --- | --- |
| `290-300` | `.icon-btn, .menu-btn` 桌面端基础规则 | `width: 38px; height: 38px` |
| `306-309` | `svg` 图标基础规则 | `width: 18px; height: 18px` |
| `4411-4415` | `@media (max-width: 767px)` 手机端 | `width: 44px; height: 44px` |
| `4424-4430` | `@media (max-height: 450px) and (pointer: coarse)` 横屏触控设备 | `width: 44px; height: 44px` |

| 位置（frontend/index.html 行号） | ID/选择器 | 作用 |
| --- | --- | --- |
| `frontend/index.html:11` | `#menuBtn.menu-btn` | 移动端菜单按钮 |
| `frontend/index.html:28` | `#themeBtn.icon-btn` | 主题切换按钮 |

| 专项测试 | 文件 | 通过数 |
| --- | --- | --- |
| 移动端触控目标 | `scripts/test-mobile-touch-targets.js` | 7/7 |

## 玩家建议卡片简化与后台图片大图（v2026-07-16）

### JavaScript（frontend/js/main.js）

| 行号 | 函数/区域 | 作用 |
| --- | --- | --- |
| `834` | `getRequestAdminDetail()` | 按 `adminReply` / `rejectReason` 生成管理员说明；done→「完成说明」，rejected→「拒绝原因」，pending/planned→「管理员回复」；空值返回 `null`。前台 `renderRequests` 和后台 `renderRequestAdminCard` 均调用。 |
| `1121` | `renderRequestAdminCard()` | 后台建议卡片。含 `admin-request-thumb` 缩略图；`aria-label="查看大图…"`、`title="点击查看大图"`、`data-open-image`、`data-gallery="admin-request-…"`。空 `images` 数组不渲染图片区。 |
| `2113` | `renderRequests()` | 前台建议列表。直接展示正文、管理员说明（通过 `getRequestAdminDetail`）、同意/否定/讨论按钮。**无** `查看详情`/`收起详情`/`request-detail`。 |
| `3760` | `openImageViewer()` | 大图查看器，复用 `imageViewer` 节点。操作 `imageViewerPrev`/`Next` 禁用/启用、更新计数和标题。 |
| `3999-4000` | `imageViewerPrev/Next addEventListener` | 上一张、下一张按钮绑定 `moveImageViewer`。 |
| `4060-4072` | `data-open-image` 点击委托 | 全局事件捕获 `closest('[data-open-image]')`；构建图片画廊并调用 `openImageViewer`。对 `template-`、`play-` 等 gallery 前缀做特殊处理；其余按 `data-gallery` 收集同画廊图片。 |

### CSS（frontend/css/main.css）

| 行号 | 选择器/规则 | 作用 |
| --- | --- | --- |
| `1820` | `.request-card h3` | 标题 17px/1.4/700。 |
| `1831` | `.request-card p` | 正文 14px/1.7。 |
| `1846` | `.vote-row` | `flex-wrap: wrap`，空间不足时换行。 |
| `1856` | `.vote-actions` | `gap: 8px`，允许按钮组自然换行。 |
| `1863` | `.request-card .mini-btn` | `white-space: nowrap`、`flex-shrink: 0`，禁止按钮文字竖排和被压缩。 |
| `2748-2758` | `.admin-note` | 管理员说明 14px/1.6/600，圆角底色。 |
| `2677-2698` | `.admin-request-thumb`、`.admin-request-thumb::after` | 后台缩略图专用样式；`cursor: zoom-in`，伪元素显示"查看大图"。 |
| `2700-2709` | `.admin-request-thumb:hover`、`.admin-request-thumb:focus-visible` 及对应伪元素 | hover/focus 时缩放、显示品牌色阴影和"查看大图"提示。 |
| `4398-4412` | `@media (max-width: 767px)` 内 `.mini-btn` | 手机端 `min-height: 44px`。 |
| `4475-4483` | `@media (max-height: 450px) and (pointer: coarse)` 内 `.mini-btn` | 横屏粗指针 `min-height: 44px`。 |

### 专项测试

| 文件 | 分组 | 通过数 |
| --- | --- | --- |
| `scripts/test-request-detail.js` | JS 28 项 + CSS 10 项 = 38 项 | `--group=js: 28/28`，`--group=css: 10/10`，`--group=all: 38/38` |

### 后台大图查看

- **无第二套弹窗**：复用 HTML 中 `id="imageViewer"` 节点和 `openImageViewer()`。
- 组件级验证：上一张（`imageViewerPrev`）、下一张（`imageViewerNext`）、关闭（`closeImageViewer`）正常。
- 后台图片 gallery 使用独立前缀 `admin-request-`，与前台 `request-`、玩法 `play-`、模板 `template-` 互不冲突。
- `data-open-image` 全局委托已支持多图画廊。
- `Escape` 当前不能关闭大图查看器，属于现有功能限制。

### 关于旧详情方案

`expandedRequestId`、`buildRequestDetail`、`resetExpandedRequest`、`toggleRequestDetail`、`request-detail` DOM 及 `toggle-request-detail` 标记已全部删除。前台不再有「查看详情 / 收起详情」功能和入口。

## 后台功能

| 位置（HTML 行号） | 函数/区域 | 作用 |
| --- | --- | --- |
| `4805` 附近 | 后台左侧菜单 | `data-panel` 切换面板（当前 13 个面板入口）。 |
| `5271` | `renderOverviewDetails()` | 运营总览。 |
| `6050` | `renderRequestManagePanel()` | **建议处理**（三区：待讨论→已完成→已拒绝）。 |
| `6338` | `renderPlayEditor()` | 玩法内容编辑器。 |
| `6371` | `renderTemplateEditor()` | 建筑模板编辑器。 |
| `6409` | `renderUpdateEditor()` | 长期/限时活动编辑器。 |
| `7112` | 保存展示开关等 | 首页内容、玩家群、服务器信息。 |
| `7925` | `renderBackupPanel()` | **备份恢复**（含自动备份、模块列表确认、重置输入确认）。 |
| `—` | `renderImageLibraryPanel()` | **图片库**（卡片式上传区+橙色复制按钮）。 |
| `—` | `loadLogs()` | **操作日志**（加载/筛选/分页/高风险标记）。 |
| `—` | `isHighRiskAction()` | 高风险操作检测（删除/导入/重置/批量删除）。 |
| `—` | `renderPanel()` | 面板总入口，根据面板名渲染并绑定事件。 |

> 行号 `—` 表示该函数存在但行号可能随版本变化，建议用全文搜索精确定位。

## 专项测试脚本

共 15 个脚本，位于 `scripts/` 目录：

```
test-settings-panel.js       - 设置面板（15项）
test-login-persistence.js    - 登录持久化（9项）
test-home-structure.js       - 首页结构（15项）
test-card-forms.js           - 首页卡片表单（26项）
test-play-content.js         - 玩法内容（9项）
test-group-entry.js          - 玩家群入口（7项）
test-template-content.js     - 建筑模板（9项）
test-activity-content.js     - 活动内容（10项）
test-activity-overflow.js    - 窄屏防溢出（7项）
test-activity-merge.js       - 数据重复合并（10项）
test-activity-delete.js      - 活动删除同步（10项）
test-request-manage.js       - 建议处理（19项）
test-image-library.js        - 图片库（21项）
test-backup-safety.js        - 备份恢复安全（19项）
test-operation-logs.js       - 操作日志（32项）
```

## 同步检查脚本

**文件**：`scripts/check-frontend-sync.js`

检测 `erp14-server-showcase.html` 与 `frontend/` 拆分源码是否同步。

**检查项**（共约 80 项）：
- 文件存在性
- CSS/JS 行数近似度
- 关键函数在 HTML 和 main.js 中同时存在（约 28 个函数）
- DOM 结构关键元素在 index.html 中存在
- 13 个面板入口在 HTML 和 index.html 中都存在
- 主要 CSS 类名一致性
- 常用 DOM id 一致性

**使用方法**：
```bash
node scripts/check-frontend-sync.js
```

输出示例：
```
=== 同步检查：erp14-server-showcase.html vs frontend/ ===
  ✓ 生产文件 erp14-server-showcase.html 存在
  ✓ 函数 "renderRequestManagePanel" 在 HTML 和 main.js 中都存在
  ...
  ⚠ DOM id "logList" 仅存在于 HTML（JS 动态生成，index.html 不含 — 预期差异）
  ...
=== 检查结果 ===
  通过: 79
  预期提示: 5
  真实失败: 0
  >>> 同步检查通过 <<<
```

> **注意**：少数 DOM id（如 `logList`、`selectAllImages`）是 JS 动态生成的，不在静态 `index.html` 中，脚本会用 `⚠` 标记为预期提示，不影响同步检查结论。


## 生产页预检脚本

**文件**：`scripts/build-frontend-preview.js`

将 `frontend/` 拆分源码合并为单页预览 HTML，用于检查拆分后的代码合并后是否正常。

**功能**：
- 读取 `frontend/index.html`（HTML 结构）
- 读取 `frontend/css/main.css`（内嵌到 `<style>`）
- 读取 `frontend/js/main.js`（内嵌到 `<script>`）
- 输出到 `dist/erp14-server-showcase.preview.html`

**注意**：
- 此文件**仅供预检**，不是正式部署文件。
- **正式生产文件仍是 `erp14-server-showcase.html`**。
- 不覆盖 `erp14-server-showcase.html`。
- 不修改 `frontend/` 下的任何文件。

**使用方法**：
```bash
node scripts/build-frontend-preview.js
```

## 一键验证总脚本

**文件**：`scripts/verify-all.js`

统一验证入口，一个小步任务完成后优先运行这个总脚本，而不是手工分别运行多条命令。

**运行方式**：
```bash
node scripts/verify-all.js
```

**执行顺序**：
1. 同步检查（`check-frontend-sync.js`）
2. 生产页预检（`build-frontend-preview.js`）
3. preview 构建测试（`test-build-frontend-preview.js`）
4. HTML 内联 JS 语法检查
5. `frontend/js/main.js` 语法检查
6. 后端测试（`backend` 目录 `npm test`）
7. 全部 `scripts/test-*.js` 专项测试（跳过第 3 步已运行的）

**注意**：
- 任意步骤失败时立即停止，不会假通过。
- 不修改业务代码，不覆盖生产页，不部署。

## 活动报名前台体验优化（v2026-07-15）

### 状态判断（main.js）

| 行号 | 函数/常量 | 作用 |
| --- | --- | --- |
| 796 | `isSignupVisible(item)` | 统一报名可见性：published!==false、signupEnabled===true、截止未来有效；被renderUpdates倒计时和renderActivitySignups共用。已结束/已颁奖/已截止等不可报名状态由renderActivitySignups中signupCanStates+getSignupTimeState共同限制。 |
| 928 | `fetchWithFallback()` | API降级封装。L932用`typeof FALLBACK_MAP`安全访问，拆分源码中无该定义时保留原始错误。 |
| 2166 | `renderActivitySignups()` | 渲染首页活动侧栏。列出published+signup项，按7态渲染徽章/按钮/倒计时。按钮调用`isSignupVisible`+状态集。 |
| 2274 | `getActivityStatusBadge(item)` | 7态徽章HTML，统一调用`getSignupTimeState`。 |
| 2285 | `EVENT_SIGNUP_STORE_KEY` | localStorage键 `erp14-event-signups`。 |
| 2286-2288 | `DAY_MS, HOUR_MS, MIN_MS, SEC_MS` | 时间常量。 |
| 2292 | `getSignupTimeState(item, now)` | 纯函数。返回7种状态之一：常驻/已颁奖/已结束/报名已截止/即将截止/进行中/报名中。8级优先级，results→已颁奖优先于status。 |
| 2311 | `formatSignupCountdown(ms)` | >24h「X天 X小时」、≤24h「HH:MM:SS」。 |
| 2325 | `readLocalEventSignups()` | 安全读取`erp14-event-signups` JSON；异常返回`{}`。 |
| 2337 | `saveLocalEventSignup(eventId, playerName)` | 仅报名成功后写。异常吞掉不影响流程。 |
| 2349 | `isLocalPlayerSignedUp(eventId, playerName)` | 当前浏览器玩家是否已报名；名称trim+toLowerCase比较；空玩家名返回false。 |
| 2359 | `startSignupCountdownTimer()` | 全局唯一定时器，每秒调`updateSignupCountdownNodes`。已存在则跳过。 |
| 2364 | `updateSignupCountdownNodes()` | 查询`[data-countdown]`节点，按`data-countdown-state`稳定标记比较；open→closing/ended时触发一次`renderActivitySignups`+`renderUpdates`。 |
| 2396 | `renderUpdates()` | 每周活动卡，调用`isSignupVisible`决定倒计时。 |
| 2496 | `submitEventSignup(event)` | 报名提交。成功后在`closeEventSignup`前保存标题，写本地记录，toast含真实活动名。 |

### 7态优先级

1. 常驻 → 常驻 2. results>0 → 已颁奖 3. status已结束 → 已结束 4. eventEndAt已过 → 已结束 5. 截止已过 → 报名已截止 6. 截止≤24h → 即将截止 7. status进行/开始 → 进行中 8. 其他 → 报名中

### 样式（CSS）

| 行号 | 选择器 | 作用 |
| --- | --- | --- |
| 676 | `.signup-cta` | 基础CTA按钮视觉与交互样式（未固定44px触控高度）。 |
| 702 | `.signup-row` | 报名信息行flex布局。 |
| 926 | `.signup-deadline` | 倒计时/截止文字。 |
| 1124 | `.signup-count` | 已报人数。 |
| 1914 | `.update-status-badge.closing` | 即将截止状态。 |
| 1915 | `.update-status-badge.closed` | 报名已截止状态。 |
| 1917-1924 | `.signup-cta.signed` | 玩家已报名按钮（禁用态）。 |
| 4410 | `.signup-cta`（手机块内） | 手机端`max-width:767px`内min-height:44px。 |
| 4440 | `.signup-cta`（横屏粗指针内） | `@media (max-height:450px) and (pointer:coarse)`内min-height:44px。 |

### 测试

| 文件 | 数量 | 内容 |
| --- | --- | --- |
| `scripts/test-activity-signup-ux.js` | 46项 | 7态、优先级、倒计时、本地记录、按钮规则、FALLBACK_MAP、signupEnabled、边界重绘。 |
| `scripts/test-mobile-touch-targets.js` | 9项 | 手机44px、桌面38px、横屏粗指针CTA≥44。 |

### localStorage键

- `erp14-player-name`：现有玩家名。
- `erp14-event-signups`：JSON `{eventId: {playerName, recordedAt}}`。仅成功后写入。清缓存/换设备丢失。非账号系统。

### 生产页SHA256（验收基准）

4b18022b8c98ccb2d4112a8fc6d78e9947843401bb8cdabc6abfc1e127e9010a

## 修改代码的建议流程

为了保证 `erp14-server-showcase.html` 与 `frontend/` 保持一致，建议按以下顺序操作：

1. **改主源**：先改 `erp14-server-showcase.html`（所有功能修改以此文件为准）。
2. **同步 frontend/**：将对应改动同步到 `frontend/`：
   - CSS 改 → 同步到 `frontend/css/main.css`
   - HTML 结构改 → 同步到 `frontend/index.html`
   - JS 逻辑改 → 同步到 `frontend/js/main.js`
3. **跑同步检查**：`node scripts/check-frontend-sync.js`，确认无报告差异。
4. **跑验证**：`node scripts/verify-all.js`，确认全部验证通过。
5. **记录日志**：在 `更新日志.md` 中记录本次修改。

## 备份与部署说明

- **备份目录**：`备份/`，按阶段和时间戳命名。
- **所有修改均未部署生产服务器**，仅在本地环境验证。
- 如需部署，建议先做服务器快照，再上传 `erp14-server-showcase.html`（单文件即可运行，无需 `frontend/`）。
